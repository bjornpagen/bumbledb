# TODO — current pin only

The living list. Older campaign residue is git history.

## Open

- **1.0.0 close** — owner ceremony.
- **Overlap constants re-pin**: `OVERLAP_CROSSOVER = 16`
  (`exec/run/overlap_leaf.rs`) and `FLAT_SWEEP_CEILING = 128`
  (`interval/overlap.rs`) stay provisional until a quiet-machine
  `overlap_profile` sweep.
- **Feature-register triggers**: C19 balance laws (`Sum == Sum` per
  group); temporal capacity (per-instant stabbing-set windows); Min/Max
  window refusal.

## This night

`bench-out/night-2026-08-20` at `4dd1ee96` (crate 0.15.0, Apple M2 Max,
shared-machine). Durable read geomean **27.9×** (2.6×–419×). Scenarios
**20.7×** over 31 timed comparisons. Compacted storage **167 / 228 B/fact**.
Charts in `assets/`. README Performance is the published copy.
