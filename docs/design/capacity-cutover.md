# Capacity cutover — the impact dossier

STATUS: LANDED (2026-07-25) — executed whole by the capacity campaign
(code + proofs + gate `b2584bcb..df7c25e2`; ships as 0.8.0). Ground-truth
deltas recorded at close, each per the design's intent:

- **§ 2.3's weighted roster landed as SEVEN corpus cases + two recorded
  out-of-lane homes** (the dossier's ~9): `capacity-closed-extension-sum-refuted`
  resolved per its own caveat — the both-sides-closed weighted refutation is a
  VALIDATION refusal (a refused schema never reaches a commit verdict), covered by
  `rejects_a_weighted_closed_pair_the_axioms_refute_under_a_dependent_bound`
  (`crates/bumbledb/src/schema/tests/reject.rs`); and
  `capacity-weight-on-fresh-keyed-relation` cannot spell a three-way case (the
  naive lane cannot utter `Generation::Fresh`), covered engine-side by the
  marks.rs "R16 interplay" unit family. Both are recorded in the conformance
  README and the roster doc comment.
- **§ 7 item 17** resolved: theorygen draws weight and hi-slot bound free and
  the querygen ledger gained the capacity tag-budget entry (C13) — the
  hand-fixture + conformance + generator-censused coverage is the recorded
  answer; **§ 7 item 18** resolved by widening: both differential twins carry
  the witnessed measure whole (C14).
- **C17 slot-vs-fetch RESOLVED by measurement (2026-08-01)**: the
  power-budget lane ran both arms (same protocol, each arm oracle-verified
  over the full corpus before timing) and the SLOT arm won every weighted
  row — `commit_capacity_sum` 32.3 vs 35.2 µs, `commit_capacity_duration`
  30.8 vs 34.2 µs (min-of-3 ephemeral p50s; the fsync-shadowed durable lane
  agreed in direction). The slot arm is the only form, the fetch arm and the
  `CAPACITY_WEIGHT_SLOT` flag are deleted, the numbers live at the
  CONSTRAINT comment (`storage/commit/judgment.rs`). The write-time ray-corner
  ruling (strictly stronger than C10's judge-time refusal — documented at
  the constraint) is now LIVE semantics and OWED to the owner.
- **C10 ray-Duration refusal parity engine-vs-naive is designed but
  untested** — the naive twin panics fixture-bug-loud on a ray reaching a
  Duration law and no lane seeds rays; one differential fixture is owed once
  the engine's refusal shape is pinned stable.

Original dossier text follows. IMPACT MAP — companion to `capacity-laws.md`
(the settled design; its six rulings are assumed, not re-argued). This is the exhaustive change map the spec flush and the code
campaign execute from: every site that moves, stated as the before/after pair with its
file:line or doc §, plus the seams the design left underdetermined (§ 7). The mentality is
the owner's standing policy verbatim: **hard cutover, zero backwards compat** — nothing is
preserved, aliased, dual-read, or migration-shimmed; old wire tags die, fingerprints move,
the format version bumps once, the corpus re-baselines, every spelling migrates in the
same campaign. Where a compat shim would be the easy answer, this dossier names it and
marks it forbidden. All line numbers verified against the working tree at HEAD `89086d4f`
(2026-07-24); load-bearing claims were independently re-verified by adversarial
spot-check, and the verifier's corrections are folded in.

## 1. Normative docs — section-by-section before/after

### 1.1 docs/architecture/30-dependencies.md (~19 sites; the design's § 7 four-site list undercounts)

| § | today | after |
|---|---|---|
| :4-5 intro | "exactly **three statement forms** — … and the one extension form (the cardinality window)" | "…and the one extension form (the capacity statement)". No "formerly cardinality window" parenthetical — greps re-target `capacity`. |
| :194-247 the extension form, whole | "**Cardinality window.** `B(Y \| ψ) <={lo..hi} A(X \| φ)` … the count of selected source facts … lies in the window" + count-flavored legal spellings ("{lo..*} floors (lo ≥ 2)", "{1..*} is the bare containment respelled"), the {0} exclusion footnote (:224-231), ladder citations (:215-222), E1 single-atoms (:233-236) | "**Capacity statement.** `B(Y \| ψ) <=[w]{lo..hi} A(X \| φ)`" — per selected target fact, the group's MEASURE (Σ weight over deduplicated φ-selected source facts; absent bracket = unit weight = count, the `<={lo..hi}` utterance surviving character-for-character) lies in the window. States the completed family as one ladder (`==` ⊂ `<=` ⊂ `<={w}` ⊂ `<=[m]{w}`), written aggregate-first with count derived (`length = sum ∘ map(const 1)`) — no legacy framing. Cites `Capacity.lean: CapacityLaw`, `Statement.capacity`, `capacity_zero_star`, `capacity_of_empty_parent`. Legal spellings split per-aggregate ({1..*} banned on the unit instance only; "floors (lo ≥ 2)" is Count-only). The {0} footnote keeps its text and gains the Sum companion (`Sum in {0}` is weaker — zero-weight rows may exist) + the loud lower-bound footgun. New paragraphs: weight typing (u64 SOURCE field / Duration(field); signed unrepresentable — polarity), dependent bounds (BOUND = literal \| u64 TARGET-row field \| Duration(TARGET field)), the composition idiom (path weights `[a.b]` typed-refused, diagnostic names the pinned-column pair). E1 + `joined_window_form_uninhabitable` survive verbatim; ladder names `window_floor_containment`/`keyed_eq_unit_window`/`unit_window_containsEq` survive as Count-instance rungs. |
| :238-247 acceptance/judgment | "seals the statement's plan handle … (`Oracle.lean: cardinality_plan_decides`): per touched parent one keyed parent probe and one child-group walk" | Same plan shape (touched_parents marks groups, weight-agnostic), citing `capacity_plan_decides`; the walk becomes "one keyed parent probe and one child-group measure walk (weight read from the reverse-index value slot, early-exit under monotone non-negative sums)". |
| :272-276 operator list, :295 example | "`<={lo..hi}` for the cardinality window …"; `Holder(id) <={1..4} Account(holder \| kind == {Checking, Savings});` | "`<=[w]{lo..hi}` for the capacity statement (absent `[w]` = unit weight, the count instance; `[field]` / `[Duration(field)]` the measure; bounds admit TARGET-row fields)". The :295 line survives byte-for-byte; the block gains one weighted line so the new productions are exercised. |
| :321-326 not-sugar argument | "(The window's `<={lo..hi}` annotation is not that rejected sugar … `Cardinality.lean: CardinalityWindow` … `in lo..hi per` is deleted vocabulary…)" | Same argument for the capacity operator, citing `Capacity.lean: CapacityLaw`; the in-per tombstone sentence survives verbatim. |
| :339-342 AdmissibleForm | "Five forms inhabit it (… the window …)" | "…the capacity statement…" — still five inhabitants; `joined_window_form_uninhabitable` citation survives (Countermodels.lean outlives Cardinality.lean). |
| :385-405 acceptance-gate bullet | "**Cardinality window:** first the canonical window vocabulary (…`CardinalityInvertedWindow`… `CardinalityContainmentWindow`…), then shared side shapes, `resolve_target_key` … and the v0 interval refusal (`CardinalityIntervalPosition`)" | "**Capacity:**" — vocabulary WITH the weight-sensitivity law (a ban is canonical-utterance policing when weight-independent, semantic deduplication when not; containment-respelled fires on Count only), then WEIGHT TYPING (u64 source field / Duration; signed + path `[a.b]` typed refusals, path diagnostic names the pinned-column idiom), then DEPENDENT-BOUND typing (u64 / Duration position of TARGET's row), then `resolve_target_key` reused verbatim + `capacity_plan_decides`; the v0 interval refusal narrowed to PROJECTIONS (intervals enter through the measure argument, never the group key). Error tokens re-mint capacity-side — keeping the `Cardinality*` identifiers "since only the name is old" is the forbidden shim (R5). |
| :406-412, :716 both-sides-closed | "a parent axiom whose child count falls outside its window"; "(containment and window forms alike)" | "whose group MEASURE falls outside its window" — the decidable refutation sums φ-surviving axiom weights; "(containment and capacity forms alike)". |
| :426-435 arenas | "keys, containments, windows"; "`KeyId`, `ContainmentId`, and `WindowId`… (windows through `Schema::window`)" | "keys, containments, capacities"; "`CapacityId`… (capacities through `Schema::capacity`)". |
| :637-651 commit-pipeline bullet | "Cardinality window: per **touched parent**… one ordered walk of the statement's `R` bucket counts the child group… A count outside the window convicts…" | "Capacity: per touched parent… one ordered walk of the statement's `R` bucket SUMS the child group's weights from the entries' value slots (u128 accumulator; `capacity_plan_decides` — the walk's measure verdict IS the delta-restricted check; early exit sound under non-negative weights: ceiling exits at sum > hi, floor at sum ≥ lo). A measure outside the window convicts with the statement id, the parent fact's bytes, and the witnessed measure. A floored Count instance may share the containment's probe machinery (`window_floor_containment`) but never skips its own check." |
| :60, :174-180, :188-189, :332, :705-706 scattered | "cardinality-window target pairs", "a window's TARGET", render_rejection's `count?`, "a containment and a window renders both citations", "touched-window term", validation-roster window rows | All re-word to capacity; `count?` → `measure?`; the roster (contractually exhaustive) GAINS the new rejection rows: signed/non-u64 weight, path weight (naming the idiom), dependent-bound field not a u64/Duration position of TARGET — see § 7 (roster enumeration risk). |

### 1.2 docs/architecture/70-api.md

| § | today | after |
|---|---|---|
| :116-123 grammar bullet | "`B(…) <={lo..hi} A(…);` (the cardinality window … bounds are non-negative integers, `*` for no ceiling …)" | "`B(…) <=[weight]{lo..hi} A(…);` (the capacity statement — optional `[field]` / `[Duration(field)]` weight between `<=` and the brace, absent = unit weight, the count instance spelled exactly as before; bounds non-negative integer literals, `*`, or u64/Duration fields of the TARGET row; spelling law below, now per-aggregate where weight-sensitive)." |
| :141-173 canonical-utterance law home | :144 "greps are total (every window is `<={`…)"; the 7-row ban table (:151-159); survivors ":161-162 `{lo..*}` floors (lo ≥ 2)"; error tokens :164-165 (`Cardinality*` names) | The law's home restates around the aggregate form and gains its general statement (weight-independent = policing, weight-sensitive = per-aggregate deduplication). Grep-totality re-derives as two keys: every capacity statement is `<=` followed by `[` or `{`. The table is RESTATED as the two-column Count/Sum split from design § 6 — not the old table plus a "weighted exceptions" footnote (forbidden). `{1..*}` splits: unit banned (containment respelled), `<=[w]{1..*}` legal. `{0}` gains the Sum-is-weaker note. Tokens re-mint capacity-named; ContainmentRespelled documented Count-only. |
| :295-303, :309, :313, :332 spec vocabulary | "`Cardinality { window }` with `WindowSpec` (`Exact(n)`, `Range{lo,hi}`, `Floor(lo)`)"; "window spellings"; parity test "every legal window spelling" | "`Capacity { weight, window }`" — bounds literal-or-target-field; the 3-kind WindowSpec's double-spelling dies (atMost→range{lo:0}, none→exact 0 — the free win: one wire vocabulary). Parity sentence → "every legal capacity spelling — unit, weighted, Duration-weighted, dependent-bound". |
| :997 builders | "(`contained`, `mirrors`, `window`, query joins)" | "(`contained`, `mirrors`, `capacity`, query joins)" — one positional builder; exporting `window` as a deprecated alias of unit-weight `capacity` is forbidden. |

### 1.3 docs/architecture/10-data-model.md

| § | today | after |
|---|---|---|
| :581-592 fingerprint inputs | "…statement-form tag (functionality = 0, containment = 1, cardinality window = 2)…; a window adds its lower bound and the upper bound's presence tag … between its sides." | "…(functionality = 0, containment = 1, capacity = N)…; a capacity statement adds, between its sides: the weight descriptor (absence tag, or field id with a Duration marker) and each bound as a kind tag (literal / target-field / Duration(target-field)) plus payload." Tag number N is an open ruling posed **keep 2 vs mint 4 — never 3**: fingerprint.rs:25-34 records v3 assigned tag 3 to the order mark and v4 retired it; reissuing 3 is the exact move the never-reissue reading forbids (verifier correction). The label bump moves every fingerprint regardless. Encoding unit-weight statements byte-identically to the old window body so count-only schemas keep fingerprints is forbidden — no dual encoding. |
| :598-599 witness list | "`KeyId`, `ContainmentId`, `WindowId`, and `OrderId` are different, validation-minted witnesses" | "`KeyId`, `ContainmentId`, and `CapacityId`…" — and drop `OrderId` in passing (pre-existing drift: no engine OrderId since the order purge; only test-schema newtypes remain). |

### 1.4 docs/architecture/50-storage.md

| § | today | after |
|---|---|---|
| :63-68 key-layout table, :186-192 | `R \| statement \| key \| source_rel \| source_row -> ()`; "a **cardinality window** writes one edge per φ-satisfying child fact … the window judgment's child-group count is one prefix walk (reader: `check_windows` …)" | The `R` row gains the weighted value-slot arm: containment and unit-weight capacity edges keep `-> ()`; a WEIGHTED capacity statement's edges carry the child's u64 weight (`-> weight_u64`), paid once at write time — the judge reads the walk it already does, never a per-child fetch (R2: fetch fallbacks do not exist). A dual-format read arm tolerating old empty-valued entries is forbidden — the arm rides the format bump; pre-cutover stores never open. (Selector scope — statement- vs relation-scoped — is a § 7 ruling; the sentence here follows it.) |
| :201-225 format-version law | history ends "Version 6 is the one id allocator (R16…)" | Gains the terminal entry: "Version 7 is the capacity cutover: the canonical schema encoding moved (weight descriptor, dependent bounds) and the `R` namespace gained the weighted value-slot arm, so every v6 fingerprint and every weighted-statement `R` entry decodes wrong." One bump covers both. |
| :359-371 judgment section | "**Cardinality windows:** every TOUCHED parent … counts the child group by one ordered walk … stopped as soon as the verdict is decided (`cardinality_plan_decides`, `window_plan_consultations`). A closed CHILD set … counted by an honest ≤256-row extension scan. A count under the floor or over the ceiling records a violation … the observed count." | "**Capacity:** every TOUCHED parent … probes, resolves any dependent bound from the parent's row, then MEASURES the child group by one ordered walk summing value-slot weights in u128 (unit statements sum 1s), stopped as soon as the verdict is decided — sound early exit because non-negative weights make the running sum monotone (`capacity_plan_decides` + the renamed consultation lemma). Closed CHILD sets are summed by the honest ≤256-row scan. A measure outside the window records the statement id, the parent fact's bytes, and the witnessed measure." |
| :830-833 write cost | "…one reverse edge (`R`) per satisfied containment direction and per window whose φ the fact satisfies." | "…and per capacity statement whose φ the fact satisfies (a weighted statement's edge additionally carries one u64 in the value slot — the write-time rent for the measure walk)." |

### 1.5 The rest of the doc estate

| site | today | after |
|---|---|---|
| docs/architecture/README.md:48, :148-149 | reading guide "`Dependencies.lean`+`Cardinality.lean`+`Txn.lean`"; "(functionality, containment, the cardinality window)" | "`…+Capacity.lean+…`" (census lane (c) checks the path); "(…, the capacity statement)". README:88 "low-cardinality-leading" is the planner sense — survives. |
| docs/architecture/60-validation.md:77, :121, :740-743 | "the window form at its boundaries"; "the child-group count per ψ-selected window parent"; "the `Task(id) <={0..8} Attempt(task)` window … the two triggers" | "the capacity form at its boundaries" + the weighted subfamilies once the corpus roster lands; "the child-group measure per ψ-selected capacity parent" (the sweep table gains the weight-desync finding); the spelling survives byte-for-byte, "the attempt window" → "the attempt-count capacity law". Census lane (c) territory — not exempt from the sweep. |
| docs/architecture/00-product.md:10 | "plus the cardinality-window extension form" | "plus the capacity extension form". |
| Root README.md:243 | "an attempt-count window" | "an attempt-count capacity law". (Other README "window"/"cardinality" hits are throughput/SQL/estimator senses — survive.) |
| ts/README.md:5, :134 | "(functionality, containment, cardinality)"; "`window`; … counts via `exactly`, `atLeast`, `atMost`, `between`, `none`" | "(functionality, containment, capacity)"; the capacity roster (`capacity`, `within`, `ref`/`duration`, `weigh`). Ships in the npm package — moves in the publish commit, not after. |
| docs/feature-register.md (zero mechanism mentions today) | 14 verdicts | Gains verdict 15: "Min/Max in capacity law position — REFUSED (typed refusal)" with the recorded trigger R4 demands (a censused workload demanding an extremal per-group law). Path weights get NO entry — R6 is a boundary, not a deferral; recording a trigger would re-open the cached-truth drift class by appointment (forbidden). |
| docs/cookbook.md + ts/COOKBOOK.md | zero statement-form usages (confirmed); ts/COOKBOOK.md:69 "…`mirrors`, window targets…" | :69 → "capacity targets". The cookbook now OWES the two recipes the design mints: calendar capacity and the power budget WITH the pinned-column composition idiom (the higher-value one — the path-refusal diagnostic names an idiom that must be spelled somewhere illustrative; Guarantee: labels pin `Capacity.lean` symbols). |
| docs/design/representation-first.md | zero mentions (confirmed) | no change. |

### 1.6 The census re-pin table (scripts/spec-census.sh lane (c); the script itself needs no code change — verified generic)

| # | old string (site) | new string |
|---|---|---|
| 1, 6 | `lean/Bumbledb/Cardinality.lean: CardinalityWindow` (30-dep:199, :323) | `lean/Bumbledb/Capacity.lean: CapacityLaw` |
| 2 | `lean/Bumbledb/Schema.lean: Statement.cardinality` (30-dep:200) | `lean/Bumbledb/Schema.lean: Statement.capacity` |
| 3, 14 | `cardinality_zero_star` (30-dep:209, 70-api:156) | `capacity_zero_star` |
| 4 | `cardinality_of_empty_parent` (30-dep:214) | `capacity_of_empty_parent` |
| 5, 7, 10, 11 | `cardinality_plan_decides` (30-dep:241, :396, :645, 50-storage:367) | `lean/Bumbledb/Oracle.lean: capacity_plan_decides` |
| 8, 12 | `window_plan_consultations` (30-dep:397, 50-storage:368) | successor name — UNRULED; `capacity_plan_consultations` is the pattern-consistent candidate (§ 7) |
| 9, 13 | bare path `lean/Bumbledb/Cardinality.lean` (30-dep:402, README:48) | `lean/Bumbledb/Capacity.lean` |

Survivors keeping their names (no re-pin): `window_floor_containment`, `keyed_eq_unit_window`, `unit_window_containsEq`, `touchedParents`, `unit_window_two_children`, `joined_window_form_uninhabitable`, `joined_window_blast`, `disjunctive_window_not_literal_conjunction` — pending the § 7 "window"-word exemption confirmation. The census is the anti-shim ratchet: the moment Cardinality.lean is deleted, lane (c) fails on every stale string — keeping the file as a re-export stub to keep docs green is the exact forbidden move. Delete the file, move all ~14 strings, same commit.

## 2. Lean spec — the theorem ledger

### 2.1 Ledger (Cardinality.lean → Capacity.lean, plus the re-pinning modules)

| symbol (site) | fate | notes |
|---|---|---|
| module doc (Cardinality.lean:1-99) | dies → NEW | Capacity.lean restated around the aggregate containment; discharge record re-pins to validate_capacity / CapacityStatement / check_capacity / capacity_plan_decides / capacity_delta_restriction. No re-export stub — Dependencies.lean:1's import moves, the file is deleted. |
| `Set.AtLeast`/`AtMost`/`ExactCount` (:108/:114/:118) | unit-lemma | Restate over the fold: `MeasureAtLeast (w) s n := ∃ l, Nodup ∧ members ∧ n ≤ natSum (l.map w)`; `MeasureAtMost := ∀ l, …` — witness-style, sound because ℕ-weights are non-negative. Count spellings survive only as `const 1` corollaries; keeping them first-class beside the measure bounds with per-site bridging iffs is the forbidden shim. |
| `atLeast_zero`, `atMost_one_of_subsingleton`, `not_atMost_one_of_two` (:122/:129/:145) | unit-lemma | First restates weight-independently (natSum [] = 0); the two subsingleton lemmas back Count-instance countermodels through the `const 1` reading. |
| `Window.admits` (:169) | dies → `admitsMeasure` | Bounds become `Bound := lit ℕ \| targetField \| targetDuration`, RESOLVED per target row before admission (`w.resolve g`). No literal-bound `Window.admits` kept for Countermodels' convenience. |
| `Window.Subsumes`, `admits_of_subsumes` (:175/:181) | restates | Pointwise over resolved bounds; decidable syntactically only on literals/identical field refs — the Bound comparison relation is a § 7 seam. |
| `zero_star_admits`, `star_subsumes` (:192/:200) | restates | Verbatim-in-shape at `lo = Bound.lit 0, hi = none`; vacuity weight-independent; same two-line proofs. |
| `window_point_admits_iff` (:208) | restates | Point bound = exact MEASURE; exact-count is the `const 1` instance. |
| `ChildGroup`, `mem_childGroup` (:222/:228) | survives verbatim | Group keying is weight-blind; all consumers (Decide:842, Txn:455, Oracle:767, Admission:472) keep spending them as-is. |
| `CardinalityWindow` (:239) | dies → `CapacityLaw` | `∀ g ∈ B, ψ g → admitsMeasure wt (w.resolve g) (ChildGroup A φ X (g.project Y))` — the ONE structural novelty is `w.resolve g`. No `abbrev CardinalityWindow := CapacityLaw Weight.unit …` alias. |
| `cardinality_of_empty_parent`, `cardinality_window_mono`, `cardinality_zero_star` (:250/:259/:268) | restates | First and third verbatim (weight/bound-independent); mono through the pointwise Subsumes. |
| `cardinality_point_exact` (:277) | unit-lemma | The general point theorem speaks of exact measure; the count reading is `weight = const 1`. |
| `Schema.lean` Window (:420-424) + `Statement.cardinality` (:455) | dies → NEW | `inductive Bound`, `inductive Weight where \| unit \| field \| duration`, `structure CapWindow`, `\| capacity (…)`. Constructor/field ORDER must be ruled once — Main.lean's decoder, the Rust serializer, the FFI marshal, and the fingerprint all pin it (§ 7). Countermodels' `joinedWindow := ⟨0, some 0⟩` (:2132) re-derives against CapWindow. |
| `Decide.childCountB`/`windowB`/`cardinalityB` (:820/:826/:834) | dies → `childMeasureB`/`capacityWindowB`/`capacityB` | `natSum ((dedupFacts …).map (weightOfB wt))` (natSum from Query/Aggregates.lean:207 — exists); capacityWindowB gains the target-row argument for bound resolution. `childMeasureB unit = childCountB`'s old value is a lemma, NOT a retained definition. |
| `childGroup_enum` (:842) | survives verbatim | |
| `windowB_iff`/`cardinalityB_iff` (:865/:887) | restates | Re-proved with the same skeleton; the load-bearing NEW lemma is the weighted pigeonhole `nodup_subset_natSum_le`. **Verifier correction: owed in TWO homes** — Decide.lean:266 (`length_le_of_nodup_of_subset`, classical erase) and Oracle.lean:377 (`nodup_subset_length_le`, split-at-occurrence) are independent count pigeonholes feeding load-bearing iffs; the Oracle proof transfers directly (needs a `natSum_append` helper, also new), the Decide erase proof needs a natSum-of-erase equation. Prove twice in local styles or consolidate — a real budgeting decision (§ 7). |
| `Decide` dispatch arms (:933/:980) | restates | `\| .capacity src wt w tgt => capacityB …`; module-doc pins at :13/:259 re-pin. |
| `Oracle` counting block (:366-435) | restates | `measureVerdict` takes RESOLVED bounds (resolution happens at the parent row before the walk verdict); `measure_admits_iff_enum` replaces `window_admits_iff_enum` — "one walk decides" survives with sum in place of length. |
| `windowPlan`/`window_plan_consultations` (:493/:555) | restates renamed | SAME plan term, SAME consultation equation — R2 (value-slot weight) means the weighted walk reads no extra entries; the equation is the theorem-level witness that R2 costs nothing at judge time. No alternate plan term modeling per-child fetches, ever. |
| `WindowPlanned`/`cardinality_plan_decides` (:742/:761) | dies → `CapacityPlanned`/`capacity_plan_decides` | Proof sketch in § 2.2. |
| `Oracle` dispatch arms (:945/:989) + module doc | restates | |
| `Txn/DeltaRestriction.lean` `touchedParents` (:436) | survives verbatim | Already covers dependent bounds: a bound-field update is remove+add of tgt.relation, the new g is ψ-selected in d.adds and its key is touched. Worth stating in the spec — it is the non-obvious reason "plan phase unchanged" survives dependent bounds. |
| `cardinalityDeltaCheck`/`cardinality_untouched_group_eq`/`cardinality_delta_restriction` (:444/:455/:482) | restates | Untouched arm transfers because the bound resolves from g ITSELF (hg₀ shows the same pre-state fact ⇒ identical resolved bound); the weight function is state-independent. Txn.lean prose (:52,:71,:130,:217) migrates. |
| `Admission.lean` `cardinalityForm`/`_denotes` (:453-562) | restates, one architecture decision | The quarantined proof survives verbatim; the Verdict field is the structural change — today `fun p _ _ ans => …` has no g in scope, but dependent bounds need the witnessed parent. Either the verdict quantifies over the false-surface answers (`∀ g ∈ ans false, ψ g → measureVerdict wt (w.resolve g) (ans true)`) or AdmissibleForm's Verdict type generalizes (§ 7). |
| `Subsumption.lean` ladder (:47/:65/:103) | restates, names kept | `window_floor_containment`'s FLOOR half generalizes to ANY weight (measureAtLeast 1 ⇒ inhabited group ⇒ reverse containment) — but the BAN fires only on Count, because only there is {1..*} equivalent to containment (a weight-0 row satisfies containment but not the sum floor). `keyed_eq_unit_window`/`unit_window_containsEq` are unit-weight by construction. |
| `Countermodels.lean` residents (:1185/:1272/:1611/:2132/:2279) | restates | Over Count-instance CapacityLaw (Weight.unit, Bound.lit) — meaning unchanged, spellings move. |
| `Bridge.lean` rows (:176-186) + :503 test strings | restates | `@Oracle.capacity_plan_decides` / `@Txn.capacity_delta_restriction` with re-pinned engine anchors and renamed test estate; the plan row's claims column states the write-time weight cost so the measured promise stays honest. Bridge rows are Name-quoted — with no alias theorems a stale row FAILS the build, the desired tripwire. |
| `Main.lean` decoder (:143-163) | restates | `decodeBound`-aware + optional `weight` key; dispatch key `"capacity"`. A dual-key decoder accepting `"cardinality"` would mask a missed re-baseline — forbidden; unknown keys error loudly. |

### 2.2 The `capacity_plan_decides` proof sketch

Today's proof (Oracle.lean:764-780): `hgrp` (answers enumerate ChildGroup, via `consult_mem` + `hfacts` + `hkey`), then `hadm := window_admits_iff_enum ∘ consult_nodup`, two lines of Iff plumbing. After: `hgrp` survives verbatim (weight-blind); `hadm` becomes per-g `measureVerdict wt (w.resolve g) (answers t) ↔ admitsMeasure wt (w.resolve g) (ChildGroup … t)` via `measure_admits_iff_enum` + `consult_nodup` — g is already bound in the quantifier at :749, so the dependent-bound read slots in with no new quantifier structure. What is genuinely harder than the count version:

1. **Weight lookup** — enum-invariance needs the weighted pigeonhole (`nodup_subset_natSum_le`, sums monotone under Nodup-subset), not the free length argument; owed in two homes plus `natSum_append` (verifier).
2. **Dependent bound read** — `hadm` is no longer uniform in t: resolved bounds vary with g, and the u64-field read must agree between the B-side resolver and the denotation (`resolveB w b = w.resolve (tupleFact b)` threads through the iff).
3. **Clipped-walk soundness** — the engine's early exit is sound by prefix-monotone natSum; the Lean plan today walks the WHOLE group and prices it. Either add a prefix-monotonicity lemma + Bridge row for the clip (the analog of `Exec.sweep_early_exit_sound`, Bridge.lean:380), or record it as mechanism-below-altitude — silence leaves the design's § 4 claim uncited (§ 7).

### 2.3 The corpus plan

**13 re-baselines, all mechanical.** Census correction (grep-verified, twice): **8 dedicated + 5 mixed**, not the design § 7's "9 + 4" (the 13 total holds; `judgment-delete-reinsert-touched-group` carries a window statement but is not named `judgment-window-*`). Dedicated: empty-parent, exact-pass, exact-under, exclusion-clean, exclusion-member, floor-by-deletion, floor-childless, set-selection-ceiling. Mixed: accept-green, statement-mix, statement-mixed-citations, delete-reinsert-touched-group, preemption-mix. Every verdict and citation index is unchanged (unit weight is the count instance character-for-character); only the statement encoding moves (key rename, Bound-tagged window, weight field). Re-baseline by grep, not by the doc's split. The Rust serializer (conformance/judgment.rs:994), the corpus files, Main.lean's decoder, and lean/conformance/README.md:212's format block move in ONE commit — byte-parity replay makes the atomicity mechanical, and there is literally no old-format read path to preserve.

**The new weighted roster (~9 cases):** capacity-sum-pass; capacity-sum-exceed-refuse; capacity-zero-weight-under-floor (the § 6 footgun as data); capacity-duration-weight (calendar shape); capacity-dependent-bound-pass / -exceed ({0..supply} read from the target row); capacity-dependent-bound-lowered-by-target-update (pins that a bound change alone re-judges the group — the remove+add path through touchedParents); capacity-closed-extension-sum-refuted (validate.rs:822 arm generalized — **check the closed-SOURCE fence at conformance/judgment.rs:30-46 before minting**; it may need to live as a validation-refusal case); capacity-weight-on-fresh-keyed-relation (R16 interplay — the weight written at mint time is seen by the same commit's walk; 50-storage.md:117/:698 seams). Blocked on the Duration-weight reader (per-fact interval-measure extractor — new machinery at the Decide altitude, § 7) and on the naive weighted judge landing first (the trophy rule: documents are written only after engine+naive agree).

## 3. Engine subsystems

### 3.1 The new Rust shapes (declared in bumbledb-theory, re-exported by the engine — the Side precedent)

```rust
pub enum Weight { Unit, Field(FieldId), DurationOf(FieldId) }   // Unit = absent bracket = the count instance
pub enum Bound  { Lit(u64), TargetField(FieldId), TargetDuration(FieldId) }  // three variants — the task brief's
                                                                // two-variant Bound cannot write the calendar law
pub struct CapacityStatement {
    pub id: StatementId,
    pub source: Side,
    pub weight: Weight,
    pub lo: Bound,
    pub hi: Option<Bound>,          // None stays the one `*` spelling
    pub target: Side,
    pub(crate) enforcement: Enforcement,
    pub(crate) checks: CompiledSides,                 // UNCHANGED — stays the shared σ pair
    pub(crate) weight_tail: Option<IntervalTail>,     // sealed DurationOf width (source_tail precedent, schema.rs:446)
    pub(crate) bound_tail: Option<IntervalTail>,      // sealed TargetDuration width
}
```

Weight representation ruling folded (cross-report seam, resolved for the dossier): **a total `Weight` with an explicit `Unit` member** — representation over optionality; the Lean `Weight.unit`, the TS wire `{ kind: "unit" }` (always present), and the descriptor encoding's absence tag all agree; `Option<Weight>` (two reports' spelling) is the same bytes but the worse struct. Owner may overrule; one form everywhere.

Dependent bounds resolve **by name against the TARGET relation's whole field roster, not the projection** — the engine determination with three reasons: (a) the judge fetches the holder fact whole before the verdict in both arms (judgment.rs:1014-1017 fresh-row F get, :1025-1029 U probe + fact_by_row, :1055 sealed extension row — verifier-confirmed), so any-field reads are one free `field_bytes`; (b) forcing the bound into the projection would demand a semantically false `Pool(id, supply) -> Pool` key through `resolve_target_key`; (c) the shared |X|=|Y| positional pair gate (validate.rs:1013) cannot carry a field with no source counterpart. **Consequence: the design doc's § 2 example spellings (`Pool(id, supply) <=[watts]{0..supply} Device(pool)`) need an errata ruling** — the target tuple stays the pure grouping key (`Pool(id) <=[watts]{0..supply} Device(pool)`) or the pair gate learns a key-prefix rule (materially larger). § 7, blocking the flush.

No compiled-offset machinery for bounds (field_bytes over the fetched fact is the universal extraction); CompiledSides gains nothing (growing it with `Option<WeightPlan>` would tax containment with capacity's shape — forbidden in the other direction). `validate_capacity` returns Enforcement plus the sealed tails (widened return or a small `SealedCapacity` carrier — pick one).

### 3.2 schema.rs / validate.rs / theory / render / errors / macros

| site | today → after |
|---|---|
| schema.rs:66-68 | `WindowId(u16)` → `CapacityId(u16)`; no `pub type WindowId = CapacityId` alias. |
| schema.rs:495-519 | `StatementRef::Cardinality(WindowId)` / `StatementView::Cardinality(…)` → `Capacity(CapacityId)` variants; id() arm respells. |
| schema.rs:550-557 + relation.rs:59-70 | `window_sources`/`window_targets` → `capacity_sources`/`capacity_targets` (fields + accessors; consumers at plan.rs:416/:433, facts.rs:209/:225). |
| schema.rs:574, :664-681, :737-738 | `Schema.windows` → `capacities`; `windows()`/`window(id)`/`window_checked` → capacity spellings; view() arm follows. No deprecated `windows()` delegating — the public API breaks, the version bumps once. |
| theory schema.rs:310-335 | `StatementDescriptor::Cardinality { source, lo: u64, hi, target }` → `Capacity { source, weight, lo: Bound, hi, target }`; doc cites `Capacity.lean: CapacityLaw`. This is a fingerprint input — the tag body re-encodes. Keeping the old variant decodable behind the format version is forbidden. |
| theory schema.rs:490-501 | `StatementKind::Cardinality` → `Capacity`. |
| theory spec.rs:143-159 | `WindowSpec` 3-kind over bare u64 → `BoundSpec { Lit(u64), Field(Box<str>), Duration(Box<str>) }`, `WeightSpec { Unit, Field, Duration }`, `CapacityWindowSpec` (exact/range/floor over BoundSpec — the as-written split survives BECAUSE the ban table needs the spelling). Names, not FieldIds — the spec is the name-level wire. |
| theory spec.rs:166-188 | `StatementSpec::Cardinality { target, window, source }` → `Capacity { target, weight, window, source }` — positional mirror of the operator. |
| theory spec.rs:334-343 (SpecIssue roster) | `WindowInverted`/`WindowExactRespelled`/`WindowExclusionRespelled`/`WindowVacuous` survive renamed `Capacity*`, firing on literal-instance shapes only (dependent bounds are per-row, not statically invertible; exact-respelled also fires on structurally identical dependent pairs {supply..supply}→{supply}); `WindowContainmentRespelled` → `CapacityContainmentRespelled`, **Count-instance-only** (fires iff `WeightSpec::Unit`); NEW `WeightPathRefused { statement, path }` — the `[a.b]` refusal whose Display names the pinned-column idiom verbatim. A dotted weight name falling through as generic UnknownField is forbidden (R6: the refusal is typed and names the idiom at every surface). |
| theory spec.rs:755-791 (Resolver::window) | → `capacity_window(…) -> (Bound, Option<Bound>)`: BoundSpecs resolve by name against TARGET's roster; ban arms gated as above; weight resolves against SOURCE, '.'-names short-circuit to WeightPathRefused. |
| theory spec.rs:979-994 (lowering) | Cardinality arm → Capacity arm; coherent() unchanged. |
| validate.rs:117-122, :195-233, :248-272 | Sealing loop: `windows` vec + `relation_window_*` → capacity spellings; arm calls `validate_capacity`, pushes CapacityStatement with sealed tails; wiring + Schema construction follow. (Doc's § 7 ":197-237" is minor drift — the arm spans :195-233.) |
| validate.rs:753-810 (validate_cardinality → validate_capacity) | Ban match generalizes (literal-gated); containment-respelled adds `&& weight == Weight::Unit`. NEW weight-typing arm: `Field(f)` requires SOURCE `ValueType::U64` — I64 is the typed signed refusal (polarity: an insert could lower a sum), everything else equally refused; `DurationOf(f)` requires Interval, seals weight_tail. NEW dependent-bound arm: TargetField must be a U64 field of TARGET's row (any field — see 3.1); TargetDuration requires Interval, seals bound_tail. `CapacityIntervalPosition` survives for projections verbatim; `resolve_target_key` + `validate_side_pair` (finding-109's unified gate) survive as-is. Keeping `validate_cardinality` as a thin wrapper for old callers is forbidden — there are no old callers. |
| validate.rs:817-864 (both-sides-closed refutation) | The `.count()` fold (:854) generalizes to Σ weight (u128); dependent bounds resolve **per parent axiom** from the sealed parent row already in hand (:834) — the refutation becomes per-axiom, a bigger rewrite than the design's one-liner suggests. A per-row-inverted resolved window at a selected parent refutes outright (pending the § 7 runtime-semantics ruling it must agree with). |
| render.rs:134-173, :205-248, :401-418, :456-479 | `RenderedStatement::Cardinality { lo: u64, hi }` → `Capacity { weight, lo: Bound, hi }`; the Display arm gains the weight bracket (Unit → nothing — the count utterance falls out, no second "legacy" printer) and a bound formatter (Lit → int; TargetField → field name via the Names trait; TargetDuration → `Duration(span)`); exact prints `{b}` on STRUCTURAL equality. Round-trip law: the rendered string re-parses under the new macro grammar. |
| render.rs:26-40, :83-91 (RenderedViolation) | `count: Option<u64>` → `measure` (width per the § 7 ruling); match arm follows. |
| error.rs:316-341 | Four `Cardinality*` StatementErrorKinds → `Capacity*` set + FOUR NEW: `CapacityWeightNotU64`, `CapacityWeightNotDuration`, `CapacityBoundNotU64`, `CapacityBoundNotDuration`. **No engine-side WeightPath kind: `Weight` carries FieldId, so a path is unrepresentable at the descriptor** — the refusal lives at the macro/spec surface only, ruling 6's representation-first shape. Keeping the `Cardinality*` names "since the payloads are identical" is forbidden (R5). |
| error.rs:993-1037 (Violation) | `Violation::Cardinality { statement, fact, count: u64 }` → `Capacity { statement, fact, measure }` — the witnessed group total; doc keeps the early-exit sentence generalized ("a ceiling conviction reports the first running sum past the ceiling") and cites `Capacity.lean: CapacityLaw`. Measure width is the § 7 ruling. |
| error/display.rs:71-115, :502-527 | law() `"cardinality"` → `"capacity"`; tail() → "a parent's child-group measure ({measure}) falls outside the window" — one arm, one message. Ban messages re-pin (`capacity_zero_star`; containment-respelled gains the weight qualifier; interval respells "in a capacity projection … the interval measure enters through the weight bracket") + four new arms naming the polarity and dependent-bound rules. |
| macros lib.rs:280-303 | `Statement::Cardinality` → `Capacity { source, weight, weight_span: Option<Span>, window, window_span, target }` — weight issues land on the bracket span. |
| macros lib.rs:864-891 (the `<` dispatch) | Gains ONE optional bracket-group peek before the brace peek: `<=[…]` → parse_weight then REQUIRE the brace (`<=[w]` alone panics naming the form); `<={…}` unchanged → Weight::Unit; bare `<=` unchanged → containment. The count spelling and the weighted spelling are ONE parse arm with an optional bracket — never an old-arm/new-arm pair. |
| macros lib.rs:951-1004 | `parse_window_bound` → `parse_bound -> BoundSpec` (int → Lit; bare ident → Field — the dependent bound; `Duration(ident)` → Duration); NEW `parse_weight` (bare ident → Field; `Duration(ident)` → Duration; `ident . ident` → the expansion panic that IS WeightPathRefused, spelling the pinned-column idiom; empty/trailing panic). `parse_window` keeps its exact shape over parse_bound. |
| macros lib.rs:902-912 (in-per tombstone) | **STAYS** — the one deliberate survivor; keyword prose stays dead. The inverse temptation (deleting it as dead code) is equally forbidden: it is a standing refusal, not residue. |
| macros lib.rs:1134-1135, :1522-1534, :1678 | `spans.windows` → `spans.capacities` + NEW `spans.weights` (bracket spans); lowering + issue-span routing follow. |
| macros lib.rs:1764-1788, :1812, :1987-2003 | Ban Display arms rename; vacuous re-cites `capacity_zero_star`; NEW WeightPathRefused arm with the two-statement example; form string `"window"` → `"capacity"`; descriptor codegen emits the Weight/Bound constructors with lowering-minted ids (nothing resolved at codegen — today's doctrine). |

### 3.3 Plan + judgment + index format + verify_store (the deepest seam; all verifier-confirmed)

**Plan phase — verbatim under new names.** `mark_ops` (plan.rs:401-447): the φ-blind source half (:416-429) and ψ-gated target half (:433-445) are already weight-agnostic — they mark groups. Weights add NOTHING to marking, provably: a weight is a row-local source field (R6), a weight change is delete+insert, both fact ops flow through the one `touched_parents` map (plan_commit :225-235 deletes, :238-248 inserts — verifier-confirmed shared path). **Dependent bounds are already covered**: a target-row bound-field update is remove+add; each ψ-satisfying half derives the SAME key tuple (key fields didn't move), the BTreeSet dedupes; the ψ-exit edge case is covered because the delete side's OLD fact satisfies ψ and marks, resolving as ψ-miss ⇒ satisfied — the no-holder doctrine verbatim. `WindowCheck { window, parent }` (:75-80) → `CapacityCheck { capacity, parent }`; `MarkEdgeOp` (:117-123) gains `weight: Option<u64>` sliced from the source fact in mark_ops — the plan stays pure over (delta, schema, selections) with zero LMDB reads. (Delete-side weight is dead work — see § 7 for the byte-vs-key-symmetric doc-comment truing.)

**The value-slot layout.** Ground truth (verifier-confirmed): every R entry's value is EMPTY today — applier.rs:164 (containment) and :175 (window) both `put_data(r_len, &[])`; every reader discards it (judgment.rs:1116 walk, reverse.rs:20-21 sweep, facts.rs:216 existence get). The weighted arm is therefore a pure addition: **R key layout unchanged** (keys.rs:326-390 — prefix walk, group boundary, parse_reverse_key byte-identical); **value = empty (containment edge, unit capacity edge) | 8-byte LE u64 (weighted capacity edge)**. The commit applier is the one writer; readers dispatch on the statement's declared weight (schema in hand at every read site), never on value length — a width disagreeing with the declaration is CorruptionError, not a fallback. "Tolerate empty values as unit weights" is doubly forbidden: pre-cutover stores never pass the format gate, so lenience could only mask a write-path bug.

**The weighted walk and the clipped-walk replacement.** `count_children` (judgment.rs:1082-1126) → `measure_children(statement, φ, parent_key, lo: u64, hi: Option<u64>) -> u128` — bounds now parameters, resolved per-parent by check_capacity. Closed arm: Σ weight over φ-survivors in the ≤256-row scan (weight = 1 | field | Duration measure), u128 accumulator, no checked arithmetic (§ 3 overflow ruling). Keyed arm: `let (k, v) = entry?`; per-entry weight 1 (unit ⇒ v must be empty) or `u64::from_le_bytes(v)` (weighted ⇒ v must be 8 bytes); running u128 sum. **`decided_at`'s replacement is the `decided(sum)` predicate**, licensed by monotonicity of a non-negative running sum: `hi = Some(h)` ⇒ exit when `sum > h` (conviction final — later entries only grow the sum; the walk reports the witnessed partial exactly as today's clipped count does); `hi = None` ⇒ exit when `sum ≥ lo` (floor satisfaction final). Unit weights behave entry-for-entry identically to today's clip. Per-child fact fetches are not a fallback — they don't exist (R2).

**check_window → check_capacity** (judgment.rs:993-1072): identical holder resolution, then dependent-bound resolution from the parent fact already in hand — **zero extra descents, verifier-confirmed**: both arms bind the full target fact before the verdict, and the fetch is not optional today (the ψ check at :1032 and the violation's fact bytes at :1066 already require it). Verdict mints `Violation::Capacity { statement, fact, measure }`. `check_windows` (:619-639) → `check_capacities`; obs span `JUDGMENT_WINDOWS` → `JUDGMENT_CAPACITIES` (one external consumer: bench sweep.rs — compile-enforced). `window_child_image` (:645-671) → `capacity_child_image`, body unchanged (weight-blind). **R16 interplay: none** — the one-allocator change affects only how a fresh-row PARENT key resolves; the R key bytes are the source projection's image regardless, and the weighted arm changes only the value slot.

**FORMAT_VERSION bump + the refuse-old-stores statement.** env.rs:62 `FORMAT_VERSION: u32 = 6` → `7`, one line. The gate (read_meta.rs:92-104) is a single equality run first on all three open lanes (verify_and_open, ephemeral probe, exhume) with no version-dispatch table — and it stays one. **Every pre-cutover store refuses to open on every lane with `FormatMismatch { found: 6, expected: 7 }`.** The R-value arm and the moved fingerprint ride this one bump. The v6 read arm ("empty R values mean unit weights, re-weigh lazily") and any exhume-lane exemption are forbidden; exhume/tests.rs:230-237 pins the refusal shape.

**Fingerprint encoder + decoder + goldens (one commit, triple-locked).** fingerprint.rs:35 label `b"bumbledb-schema-v4"` → `b"bumbledb-schema-v5"` — hashed as the stream's FIRST bytes (:77), so ALL schema fingerprints move, count-only schemas included (verifier-confirmed). The tag arm (:125-137) becomes the capacity encoding (weight descriptor tag + bounds as kind-tag + payload, between the sides); tag number per the § 7 keep-2-vs-mint-4 ruling. **descriptor_codec.rs:258-276** (the exhume decoder — the completeness sweep's highest-value miss) becomes the exact inverse; error strings re-mint ("descriptor capacity bound tag" — "window" dies with "cardinality" in mechanism diagnostics); the module's round-trip law (decode→validate→re-encode == stored bytes) makes a lagging decoder fail every exhume. A dual-label decoder arm lifting v4 count bodies into unit-weight Capacity is forbidden — and moot: pre-cutover stores never reach descriptor decode (format gate first). **Fingerprint golden tests** (fingerprint.rs:340-380, :540-656): both pinned hex constants re-derive; hardcoded label bytes move; and the statement-encoding golden GAINS the capacity arm — **the tag-2 body has never had a byte-level golden** (pre-existing hole); landing the most-changed encoding without one leaves the next codec drift invisible until an exhume fails in the field. Keeping old hex constants as a "previous format" record is forbidden — one pin per golden; the past lives in 50-storage's version history.

**verify_store.** The two shared-checker call sites (facts.rs:239 per ψ-selected ScalarProbe fact; marks.rs:37 per closed-parent axiom) ride the rename free — one definition, never a sweeper copy (:990-992 doctrine). `StoreFinding::WindowViolation` → `CapacityViolation { measure }`. **NEW invariant: the weight-desync sweep, both directions** — ruling 2 makes the index value slot a maintained second copy of one row-local field, and verify_store is exactly the offline authority that convicts a diverged copy. F→R (facts.rs:216): the existence get's value must equal the fact's weight-field encoding (unit: empty) — zero extra reads, the value came back with the probe. R→F (reverse.rs:104-109): third conjunct, value backs to the live fact. New `StoreFinding::ReverseEdgeWeightDesync { statement, reverse_key, stored, derived }`. Sweep lenience for empty values is forbidden (old data cannot reach the sweep). Repair story — fix_store arm vs convict-only — is a § 7 item. **write.rs:263-269** (`decode_cited_facts`, the rejection-exit fact decoder no dive covered): the `Violation::Cardinality`/`StatementView::Cardinality` destructure renames; comments respell.

**FFI/introspection touchpoints:** TS `explain()` is query-plan-only — no shape change. The statement-shaped surfaces are `dbManifest.statements` (kind tag `"capacity"`, spelling = the renderer's capacity form) and the Violation wire (§ 4).

## 4. Surfaces

### 4.1 The TS builder (type-level enforcement)

`ts/src/count.ts` (211 lines) **dies whole** — five constructors, both ban tiers, the `admitted` brand. Keeping them as thin wrappers over `within()` so ~90 test lines compile unchanged is the named forbidden move. What must REAPPEAR in the one capacity builder:

```ts
// two overloads — TS cannot put an optional param mid-list; positional mirror of the operator
capacity<B, W, A>(target: B, window: W & CountWindowBans<W> & BoundsOnTarget<W, B>,
                  source: A & SameArity<B,A> & SameShapes<B,A>): CapacityStatement          // unit weight
capacity<B, M, W, A>(target: B, weight: M & WeightOnSource<M, A>,
                     window: W & SumWindowBans<W> & BoundsOnTarget<W, B>,
                     source: A & SameArity<B,A> & SameShapes<B,A>): CapacityStatement       // weighted
```

- **Companion mints:** `within(n)` → exact {n} (`within(0n)` IS the exclusion's one spelling); `within(lo, hi)` → range; `within(lo, "*")` → floor; `ref(field)` / `duration(field)` for dependent bounds; `weigh(field)` / `weigh(duration(field))` for the measure. Exact shapes underdetermined by the design — § 7 (the doc's `f(Device, "watts")` sketch names a helper that does not exist).
- **BoundsOnTarget** (ref-on-target, type level): for each `Ref<K>` in W's bound slots, `K extends FaceFields<B["source"]>` AND `ProjectedShape<B["source"], K>[0] extends "u64"` (DurationRef → kind "interval") — buildable from existing face.ts machinery (FaceFields :103, ProjectedShape :158); the verdict interface names the target's field roster.
- **WeightOnSource** (u64-weight-on-source, type level): the weighed field is a field of the SOURCE face's owner, u64-kinded (Duration form: interval-kinded), checked at the capacity() call where A is inferred — the proven SameShapes constrain-after-inference pattern. Runtime twins `assertBoundOnTarget`/`assertWeightOnSource` for untyped callers.
- **Ban tiers re-derived:** type tier weight-independent (negative bound via IsNegative verbatim; lo==hi → "write within(n)"; {0..0} → "write within(0n)"; {0..*} → delete the statement); type tier weight-SENSITIVE — {1..*} banned ONLY on the unweighted overload (naming `contained`), LEGAL on the weighted one (the two overloads carry different ban intersections); construction tier re-judges computed bigints + inversion (literals only — ref() bounds carry no value). The `admitted` unique-symbol brand survives on within()/weigh() products. Bans that DIE with the partition: exactly-0→none, AtMostBan entirely, between-zero-floor→atMost (within(0n, hi) is now the canonical ceiling — the design's own example confirms).

Other ts/src sites: statements.ts:278-293 `window()` + :314 renderer arm → the capacity arm rendering `<=` + weight bracket + window with ident bounds, byte-identical to the engine renderer (bijection law); spec.ts:93-96 → `CapacityBoundSpec`/`CapacityWindowSpec`/`WeightSpec` mirroring theory spec.rs 1:1 (no deprecated WindowSpec alias — the old shape is unparseable); lower.ts:101-107 → `{ kind: "capacity", …, weight }` with weight ALWAYS present (`{ kind: "unit" }`); index.ts:43-44 count exports deleted, capacity roster exported; schema.ts:309 mint-refusal message → "key/contained/mirrors/capacity"; law.ts comments respell (pairing logic weight-blind, survives); native.ts:178 `StatementKindTag` → `"capacity"`, :241 `Violation.count?` → `measure?`; **db.ts:129-136 + :566 + :892** (the PUBLIC typed Violation the dives missed) — `count?: bigint` → `measure?`, declaredEntries' `case "window" → kind "cardinality"` → `case "capacity" → kind "capacity"` (three spellings of one tag across db.ts/native.ts/tags.rs move together). A deprecated `count` getter beside `measure` is forbidden — the SDK breaks once.

### 4.2 Wire/FFI tag cutover (ts/crate/src — note: no bumbledb-ffi crate; design § 7's path is corrected)

| site | today → after |
|---|---|
| tags.rs:177-184 | `mod window` (EXACT/RANGE/FLOOR over WindowSpec) → `mod capacity_window` (same three KIND tags, payloads now BoundSpec) + TWO NEW tables `mod capacity_bound { "lit", "field", "durationField" }`, `mod weight { "unit", "field", "durationField" }`. |
| tags.rs:191, :201 | statement + statement_kind `"cardinality"` → `"capacity"` in BOTH tables. The string "cardinality" appears NOWHERE in the crate after (R5). A parse-side alias so an old SDK build still opens is forbidden — old wire hits the existing unknown-tag refusal, the designed behavior. |
| tags.rs:345-347 | golden census rows re-key (capacityWindow, + capacityBound, weight); ts/test/fixtures/tags.json re-rendered same commit (the test enforces it). |
| marshal.rs:504-523 | `window_in` → `capacity_window_in` + `capacity_bound_in` (lit → u64_in; field/durationField → String) + `weight_in`. A bare-BigInt bound accepted as implicit lit is forbidden — the old positional shape is dead wire. |
| marshal.rs:544-548 | CARDINALITY arm → CAPACITY arm; **weight is a REQUIRED key** (req, not get) — unit crosses explicitly. |
| marshal.rs:1155, :1201-1203 | `ViolationWire.count: Option<u64>` → `measure` (width per the § 7 ruling; u128 crosses as BigInt words if ruled wide); wire key `"count"` → `"measure"`. |
| fingerprint_lock.rs:93 + ts/test/fingerprint.test.ts twin | PIN re-derives (fingerprints move); the CrossHost theory re-mints through capacity()/within() and EXTENDS with weighted + dependent-bound + Duration statements so the lock covers the new encoding surface. Accepting either fingerprint "during transition" is forbidden — one pin, moved once. |

### 4.3 Compile-fail roster (census correction: **9 files exist, not the design's 10**)

| file | fate |
|---|---|
| window_in_per_is_deleted.rs | **STAYS exactly as is** — the tombstone (name and content). Only //@ re-pins if the diagnostic hint text moves. |
| window_empty_bounds / exact_pair / inverted / open_ceiling / open_floor / zero_zero | survive renamed `capacity_*`, spellings unchanged (weight-independent bans), diagnostics re-derived; inverted is literal-literal only. |
| window_zero_star | survives renamed; //@ error re-cites `capacity_zero_star`. |
| window_one_star | **SPLITS**: refusal survives only for unweighted `<={1..*}`; a NEW passing test (not compile-fail) proves `<=[w]{1..*}` compiles — the § 6 weight-sensitive row as a positive probe. |
| NEW (7 files) | capacity_weight_path.rs (`[model.watts]` — diagnostic names the pinned-column idiom), capacity_weight_signed.rs (polarity), capacity_weight_not_u64.rs, capacity_duration_weight_not_interval.rs, capacity_bound_foreign.rs (bound field not on TARGET's row), capacity_bound_signed.rs, capacity_duration_bound_not_interval.rs. All registered in the schema_compile_fail.rs harness (:155 roster comment). The brief's "inverted-with-idents" file is only spellable if dependent lo-bounds are refused — § 7 ruling. |

### 4.4 Test-estate migration table (census correction: TS is ~90 SDK-surface lines across 7 files + 3 FFI-side files, not "8 files/84 lines")

| file | kind | count / notes |
|---|---|---|
| ts/test/statements.test.ts | SEMANTIC | 38 lines — the ban-machinery probe suite; export-roster pin → capacity module; render probes keep unit strings byte-for-byte + gain weighted rows; @ts-expect-error probes re-derive per the new table incl. the {1..*} split (a new positive compile probe); dead bans' probes deleted; NEW probes: ref() off-roster, weigh() non-u64, forged un-branded window. |
| ts/test/fixtures/law-scale.ts | MECHANICAL via generator | 21 lines — edit 3 emitter lines in ts/scripts/generate-law-scale.ts (:38/:43/:79), regenerate; never hand-edit. The 123-statement compile bar is the canary for the heavier ban types — compile early. |
| ts/test/render-golden.test.ts | SEMANTIC | 11 — re-mint via capacity(); unit goldens unchanged byte-for-byte; ADD `Pool(id) <=[watts]{0..supply} Device(pool)` + calendar goldens against the engine arm. |
| ts/test/fixtures/run-store-schema.ts | MECHANICAL | 10 — exactly(1n)→within(1n), none→within(0n), window(→capacity(. |
| ts/test/fingerprint.test.ts | SEMANTIC + pin | 6 — theory extends; both pins move together. |
| ts/test/law-typing.test.ts | MECHANICAL | 3 — atMost(3n)→within(0n,3n). |
| ts/test/db.test.ts | MECHANICAL | 1. |
| ts/test/ffi.test.ts | SEMANTIC | ~6 — raw spec reshapes ({kind:"capacity"}, weight, tagged bounds); violation.kind/measure; add a weighted hostile-caller probe. |
| ts/test/wire-tags.test.ts + fixtures/tags.json | GOLDEN | 3+3 — rosters re-baseline with tags.rs in one commit. |
| crates/bumbledb/tests/schema_macro.rs | SEMANTIC extension | 6 sites — spellings unchanged; descriptor assertions rename; module gains the new grammar's accepts (weighted, Duration, dependent, weighted floor). |
| crates/bumbledb/tests/schema_spec.rs | SEMANTIC assertions | 5 — spellings unchanged; assertions rework to Capacity/Unit shapes; gains weighted/dependent/Duration spec rows. |
| crates/bumbledb/tests/dyn_surface.rs | MECHANICAL | 3 — spelling + rendered string byte-identical; only StatementKind renames. |
| exhume.rs, witness_stability.rs, alloc_census.rs, alloc_gate.rs, adversarial_ir.rs, ramdisk_phase_r.rs, schema_compile_fail.rs | MECHANICAL | misc — spellings unchanged; comment vocabulary + pattern-matches rename; harness registers new files. |
| **in-crate estates (the completeness sweep's finds)** | | |
| src/schema/tests.rs:100 + tests/{valid,reject}.rs + render/tests.rs | SEMANTIC | the shared `cardinality(...)` fixture helper re-mints as `capacity(...)` (unit defaults keep diffs one-line — but the helper RENAMES, no test-only wrapper); every `Cardinality*` token assertion respells; Bridge-cited test fns (`a_cardinality_window_over_a_declared_key_validates` etc.) rename in lockstep with Bridge rows (census lane (b)); reject.rs GAINS the new typed-refusal unit family beside the compile-fail files. |
| src/storage/commit/tests/marks.rs | SEMANTIC extension | ~64 sites — fixtures re-encode; `assert_window_violation` → `assert_capacity_violation { measure }`; the Bridge.lean:503 test-name strings live here (`window_floor_convicts_a_childless_parent` :168, `window_removal_recounts_the_touched_parent` :264, `key_violation_preempts_the_window_judgment` :439, `statement_phase_cites_containments_and_windows_together` :474); module GAINS the weighted judge's unit family (sum ceiling/floor, zero-weight-under-floor, dependent-bound from final-state holder, duration) — the commit-path twin of the corpus roster. Ceiling tests assert the witnessed measure EXACTLY (first running sum past hi), pinning clipped-witness determinism. |
| src/verify_store/tests.rs:1571, :1638 | SEMANTIC | descriptor re-encodes; `WindowViolation` → `CapacityViolation`; GAINS the weight-desync test (hand-corrupted R value convicted) — otherwise the new finding ships untested. |

## 5. Bench + oracles

**Naive judge** (naive.rs:461-487): `window_violated` → `capacity_violated`, mirroring the Lean denotation exactly — the BTreeSet state IS dedupFacts; fold `measure: u128 = Σ weight(child)` (1 | child field | interval end−start); bounds evaluate per parent (literal | parent field | Duration) at judge time; no-parent ⇒ vacuous survives via the existing .any. `Violation::Cardinality { statement }` → `Capacity { statement }` — the naive twin stays payload-free unless the § 7 measure-parity ruling widens both twins. No count fast-path beside the general fold; no delegating `window_violated` shell. The 3 fixture sites (naive/tests/judgment.rs:1218/:1328/:1341) re-encode; NEW naive-parity subfamilies pin the § 6 Count/Sum split as data (zero-weight rows are the semantic point, not "count tests plus a multiplier").

**SQLite twin.** Estate-map correction: **design § 7's "translate/builder 6" is stale — translate/builder.rs has ZERO window sites** (its `<=` hits are interval SQL); the real estate is translate.rs:220-223/:288-290, where `Inexpressible::CardinalityJudgment` → `CapacityJudgment` with the doc extended: the weighted form is exactly as inexpressible-as-a-pinned-verdict; the naive model remains the sole capacity oracle in the differential/conformance lanes. Wiring a judged SQLite arm "because SUM is expressible" is forbidden — it would test the emulation, not the engine (the module's recorded doctrine). The one place SQLite DOES speak the law is the lawful enforcement map (enforcement.rs:140-146, the COUNT trigger): the weighted twin expression is `BEFORE INSERT ON "Device" WHEN (SELECT COALESCE(SUM("watts"),0) FROM "Device" WHERE "pool" = NEW."pool") + NEW."watts" > (SELECT "supply" FROM "Pool" WHERE "id" = NEW."pool") BEGIN SELECT RAISE(ABORT, …); END` — SUM for COUNT, dependent bound as a correlated parent subselect, NEW's own weight added (BEFORE INSERT sees pre-row state); Duration weights sum (end−start). Floor laws need a BEFORE DELETE twin — the polarity table decides the trigger set per law; the totality test (one map row per materialized statement) is the anti-drift device, and a "judged post-hoc" dodge arm is forbidden.

**The two new lanes** (windowed.rs sibling or module extension; oracle gating per the windowed/tests.rs:50-108 precedent — same deltas through differential::run vs NaiveDb, verdicts+citations compared whole BEFORE any timing; engine-only rows, theirs: None; differential/tests/marks.rs gains the weighted parity twins first — they are the oracle gate for the timed rows):

1. **Power budget** (dependent bound): twin worlds `Pool { id fresh, supply: u64 } / Device { id fresh, pool, watts: u64 }` with `Device(pool) <= Pool(id); Pool(id) <=[watts]{0..supply} Device(pool)` vs statement-free control — rows `commit_capacity_baseline` / `commit_capacity_sum` (the dependent-bound read + weighted value-slot walk on the hot path; Mass::BENCH-scale so the walk is real).
2. **Calendar capacity** (Duration weight): `Room { id fresh, span: interval<u64> } / Booking { id fresh, room, booked }` with `Room(id) <=[Duration(booked)]{0..Duration(span)} Booking(room)` — row `commit_capacity_duration`. **Home undetermined** (§ 7): fresh twin world (recommended — calendar digests unmoved) vs reshaping calendar::Scheduling's Room (re-baselines every calendar corpus digest and verify stamp; the module doc records the last such reshape as a deliberate act). § 9's "calendar-capacity shape" names the SHAPE, not necessarily the module.

Existing lanes: lawful.rs:101's `Task(id) <={0..8} Attempt(task)` survives character-for-character (unit utterance — NOT grandfathering; everything beneath it dies); lawful naming/pins re-key (`cites_capacity`, "(Capacity cited)", tests re-pin against the capacity arena with Bound terms and weight asserted Unit explicitly); windowed.rs's two spellings survive verbatim; families/write.rs registry re-comments + gains the three capacity rows; old perf pins delete (the standing rule) — the report merger must never see old and new row names as distinct live rows. Cleared non-sites: displaced.rs:620 `alloc_window`, corpus_gen/range_window.rs, querygen's Transfer.window column — measurement/interval vocabulary, over-renaming forbidden.

**querygen coverage from day one.** Ground truth: the querygen target ledger declares NO window statement (target.rs:419-527); :930 PANICS on the variant; theorygen's random_statement (:265-278) mints Cardinality for the **validate sweep only** (descriptor.validate() — never a commit verdict); the fuzz crate is hard-deleted. So "randomized differential covers the new law from day one" is currently unsatisfiable at the judged-write level — the finding-025 lesson (no unspellable-by-generator features) makes this an owner decision, not a rename (§ 7). Regardless: theorygen's arm generalizes (weight draw Unit | Field | Duration — structurally free so signed/dangling/interval-in-scalar shapes reach the typed refusals; bounds draw literal | target field with inverted/vacuous freedom; zero-valued weights stay reachable and censused: unit/weighted/duration × bounded/star + dependent-bound all asserted > 0), and :930's arm renames. The "generator-exempt" dodge is the forbidden answer.

**Conformance + re-baseline mechanics.** The 3 fixture-schema constructions (conformance/judgment.rs:145-157/:209-214/:238-249) re-encode; push_statements' serializer (:988-1003) emits the capacity JSON (`weight ∈ null|{"field":N}|{"duration":N}`, `bound ∈ int|{"field":N}|{"duration":N}` — exact spellings follow the § 7 field-order + weight-representation rulings); :1060's verdict map renames; write_corpus (conformance.rs:1873) clears-and-rewrites lean/conformance/cases, replay_checked_in_corpus (:1910) byte-compares per push — serializer, corpus, Main.lean decoder, and README § 212 move in ONE commit or the comparator fails (the atomicity is mechanical; there is no old-format read path to shim even if one wanted to). differential.rs:204's cited() arm, sqlmap.rs:82/:137-141 (fairness count + skip arm), and sweep.rs:288-367 (JUDGMENT_WINDOWS field) follow their renames.

## 6. Hard-cutover consequences, stated plainly

- **Every schema fingerprint moves** — the encoding-format label is the hash stream's first bytes (fingerprint.rs:77), so the v4→v5 label bump moves all fingerprints, count-only and statement-free schemas included; the 0.6.0 "fingerprints unmoved" note was that release's fact, not a standing law.
- **Every pre-cutover store refuses to open** — FORMAT_VERSION 6→7 through the one single-equality gate on all three open lanes (verify_and_open, ephemeral probe, exhume); there is no migration read arm today and none is added; ETL through the SDK is the story.
- **The corpus re-baselines whole** — write_corpus clears and rewrites; byte-parity replay per push makes a half-moved corpus fail loudly in both directions; the 13 cases re-encode mechanically and the weighted roster lands beside them.
- **No dual reads anywhere** — each read site keeps exactly one arm because a second arm can only mask a bug: marshal's statement_in (old tag = unknown-tag refusal, the designed behavior), Main.lean's decoder (a "cardinality" key = a missed re-baseline that must fail), descriptor_codec's label gate (pre-cutover stores can't reach it; a lifted decode would fail the round-trip re-encode anyway), the R value slot (empty-under-weighted = CorruptionError — the format gate already proved no old data is present), the fingerprint pin (one pin; open's whole schema gate).
- **Every spelling migrates in the same campaign** — the schema-layer renames do not compile until judge/FFI/tests move; the atomicity is enforced by the type system, the census (lane (c) fails on Cardinality.lean's deletion), the Bridge Name-quotes, the golden tests, and the corpus comparator — the ratchets ARE the anti-shim mechanism; weakening any of them "temporarily" is the forbidden move in each case.

## 7. Risks and owner attention items

**Blocking the spec flush (rulings needed first):**

1. **Dependent-bound spelling vs the pair gate** — the design § 2 examples (`Pool(id, supply) <=[…]{0..supply} …`) put the bound field in the target tuple, which breaks the shared |X|=|Y| positional gate (validate.rs:1013) and would force a false `Pool(id, supply) -> Pool` key. The engine determination: bound idents resolve by name against TARGET's whole roster; the written tuple stays the pure grouping key (`Pool(id) <=[watts]{0..supply} Device(pool)`). Ratify the errata (or rule the materially larger pair-gate change).
2. **Statement.capacity field order** — design § 5 writes "(agg, source, window, target)", the operator reads target-weight-window-source; the Lean constructor, corpus JSON, FFI marshal order, descriptor codec read order, and fingerprint encoding all pin it. One ruling before any file moves; rides the single format bump (the exhume round-trip triple-lock makes encoder/decoder/validator land in one commit).
3. **Violation measure width** — engine accumulates u128; today's payload/wire is u64 end-to-end; a genuine floor-conviction total can exceed u64. u128 through Violation + BigInt on the JS wire, or saturating u64 — silent truncation is not an option; the reports split three ways, rule once.
4. **Weight representation** — total `Weight::Unit` member (this dossier's fold) vs `Option<Weight>`; same bytes, one struct form everywhere (Rust, Lean, TS wire, naive twin, codec).
5. **Fingerprint statement-form tag: keep 2 vs mint 4 — never 3** (verifier correction: tag 3 is the retired order-mark tag, v3/v4 history at fingerprint.rs:25-34; reissuing it is what the never-reissue law forbids). Compatibility is moot (label bump moves everything); this is pure bookkeeping law, but 10-data-model.md:588 must state one number.
6. **Dependent-bound slot restriction** — the grammar admits a field in either bound position; inversion with idents is per-row, statically undecidable. Restrict dependent bounds to the hi slot (making the brief's "inverted-with-idents" refusal spellable at expansion) or admit both slots and rule the per-row-unsatisfiable semantics (an inverted resolved window = empty window ⇒ every ψ-selected touched parent convicts — the validate-time both-closed arm must agree with the judge either way).
7. **groupMeasure totality vs the no-finiteness posture** — the design's total `groupMeasure : ℕ` would spend the spec's first finiteness token (Cardinality.lean:18-27's stated law); the faithful shape is the witness-style MeasureAtLeast/MeasureAtMost pair, sound on non-negative ℕ. The two are not interchangeable in theorem statements — rule which Capacity.lean states.
8. **Corpus filename policy** — `judgment-window-*` stays vs re-keys to `judgment-capacity-*`; ripples into Bridge instrument tokens (census lane (b)), lean/conformance/README's worked example, and 60-validation's fixture census; the replay comparator fails a half-rename in both directions. Decide before regeneration.
9. **Validation roster enumeration** — 30-dependencies:692-719 is contractually exhaustive; the rewrite must name the exact new rejection rows, and the closed-TARGET × dependent-bound interaction (bounds read from ground axioms — per-axiom resolution at seal time) is entirely undesigned beyond this dossier's § 3.2 sketch.

**Design gaps the campaign must close (each needs one recorded decision):**

10. **Duration weight/bound over a ray interval** (end = MAX) — query-side has the typed MeasureOfRay error; a capacity weight has no answer cell. Validate-time refusal vs judge-time behavior — must be ruled before the naive judge, the Lean denotation, and the corpus can agree.
11. **Weight/bound dimension mixing** — `<=[watts]{0..Duration(span)}` and `<={0..Duration(span)}` are representable (both sides u64 under the encoding); enforce Duration↔Duration agreement in validate or document that u64 is u64.
12. **Admission.lean Verdict architecture** — the Verdict field has no g in scope; dependent bounds need the witnessed parent. Bounded quantification over the false-surface answers vs generalizing AdmissibleForm — the one place the admission architecture itself moves.
13. **Clipped-walk soundness in Lean** — named prefix-monotonicity obligation + Bridge row, or recorded mechanism-below-altitude narrowing; silence leaves the design's § 4 soundness claim uncited.
14. **The weighted pigeonhole is owed twice** (verifier) — Decide.lean:266 and Oracle.lean:377 are independently-proved count pigeonholes with different techniques, both feeding load-bearing iffs; prove `nodup_subset_natSum_le` twice in local styles or consolidate, plus the missing `natSum_append`/natSum-of-erase helpers either route needs. Budget it.
15. **Bound resolution totality** — gate-refused shapes need a total `resolve` story (precedent: the recorded-narrowing junk-total default, Dependencies.lean:266); the choice leaks into capacityB_iff's statement shape.
16. **Bound subsumption/mono under dependent bounds** — Subsumes and the respelling detector need a Bound comparison relation (pointwise vs syntactic-on-literals); the design rules the ban table but not this.
17. **Randomized judged-write coverage** — no living lane exists (fuzz deleted; theorygen validates only; querygen's world declares no capacity statement). Options: (a) seeded random-theory judged-write lane through differential::run; (b) a capacity statement in the querygen target ledger (moves every querygen digest — a deliberate act like the calendar Slot reshape); (c) rule hand-fixture + conformance sufficient and record the finding-025 tension.
18. **Measure parity in the differential** — both twins drop payloads today; after the cutover NO lane cross-checks the witnessed measure engine-vs-naive (the corpus pins it; the differential doesn't). Widen both twins or record the asymmetry. Related: the reported measure on ceiling convictions is a clipped, walk-order-dependent witness (deterministic under R key order) — state it before baselining exact measures.
19. **Calendar lane home** — fresh twin world (recommended) vs Scheduling reshape (total digest re-baseline). § 5.
20. **window_plan_consultations successor name** (`capacity_plan_consultations` is the pattern-consistent candidate) — mint before the doc rewrite or two census re-pins are guesses. Related: confirm the word "window" (as the {lo..hi} object, which genuinely survives) is exempt from the purge — four Countermodels/Subsumption census strings hang on it.
21. **R value-slot selector scope** — design § 4 says "capacity-source relations", § 8.2 says "the capacity-weighted arm" (statement-scoped). Statement-scoped is what § 3.3's design assumes (a relation sourcing both a weighted and a unit statement has per-bucket value shapes); one sentence of ruling before 50-storage is rewritten.
22. **Smaller recorded choices**: weight-desync repair (fix_store rewrites from the fact — ruling 2's direction — vs convict-only); MarkEdgeOp delete-side weight (dead work vs doc-comment re-truing to "key-symmetric"); dependent bound = key-projection field (harmless, but rule legal vs refused); `DurationOf` over a fixed-width interval (degenerate constant weight — police or not); TS `within()`/`weigh()` exact mint shapes (star sentinel, single-arg exact, weigh's argument form); bench row-name re-keying (one ruled rename, not drift); floor-walk worst case with zero-weight children (no longer bounded by window constants — a perf-story line + a zero-weight-heavy bench shape); law-scale compile-time canary (compile the regenerated fixture early); the Sum-{0} footgun's jsdoc home; SDK/engine publish ordering (the ts crate pins bumbledb by version — the 0.6.0 bootstrap-gap lesson); hardcoded `<={` greps in scripts/tests (grep-totality is now two keys).

## 8. Campaign shape

Same skeleton as design § 9, with the flush targets and lanes made concrete. **Step 0 — the § 7 blocking rulings (items 1-9), one sitting.** Then:

**Flush (docs + spec stated, one campaign):** 30-dependencies (~19 sites) + 70-api (law home restated) + 10-data-model (tag + witness list, OrderId drift fixed in passing) + 50-storage (value slot, v7 history, judgment §) + 60-validation + README/00-product/root-README/ts-README one-liners + feature-register verdict 15 + the two cookbook recipes + Capacity.lean stated with the obligations named (capacity_plan_decides, the two pigeonholes, the Admission verdict shape, the clip decision) — census lanes (b)/(c) enforce the re-pin table mechanically. Sized: one focused doc campaign, the Lean statement skeleton alongside.

**The code+proofs campaign (one atomic land — the renames don't compile piecemeal; the format bumps once):**

| lane | contents | size |
|---|---|---|
| A — theory + schema | spec.rs shapes/resolver/bans, schema.rs structs/arenas, validate_capacity (weight typing, dependent bounds, per-axiom closed refutation), render, errors/display, macros (dispatch, parse_bound/parse_weight, spans, codegen) | L |
| B — storage + judge | plan (CapacityCheck, MarkEdgeOp weight), applier value writes, check_capacity + measure_children (u128, decided(sum)), FORMAT_VERSION 7, fingerprint v5 arm + descriptor_codec inverse + goldens (incl. the first-ever tag-body byte golden), verify_store (renames + weight-desync sweep both directions), write.rs decoder | L |
| C — Lean discharge | Capacity.lean, Schema/Decide/Oracle/Txn/Admission/Subsumption/Countermodels/Dependencies re-pin arm-for-arm, the two pigeonholes + natSum_append, capacity_plan_decides, Bridge rows, Main.lean decoder | L |
| D — TS + FFI | count.ts deletion, the capacity builder + mints + type walls, spec/lower/index/db/native, tags.rs + marshal.rs + tags.json, fingerprint pins, ts test estate (~103 lines/10 files) | M-L |
| E — test estates + corpus | compile-fail (8 renames + the one_star split + 7 new), in-crate unit estates (schema/tests, marks.rs ~64 sites + weighted family, verify_store + desync test), integration spelling sites, conformance re-baseline (13 + ~9 weighted, one commit with serializer/decoder/README) | M |
| F — bench + oracles | naive capacity_violated + parity subfamilies, translate/lawful/windowed renames, the SUM trigger, differential twins, the two new lanes behind their oracle gates, theorygen weighted arm + census, querygen per ruling 17 | M |

**Then bench re-pins** (windowed/lawful re-baselines under the new format, old pins deleted, ALL graphs re-keyed) and the 0.8.0 release rides the normal process — version lockstep across the workspace, the npm publish carrying the README and the moved fingerprint pin in the same release.
