# Review identity, final qualification and retirement

## This proposal’s evidence and scope

Workspace: /Users/bjorn/Documents/bumbledb. Branch: `codex/bumbledb-1-0`. Base HEAD: `4a0573692431ae7b2f8ef82d8663bd199a4058da`. This is a large dirty tracked/untracked implementation, not the HEAD tree. Source was still moving during review; decisive session/staging/Pack/locator/scalar/lock anchors were rechecked at **2026-09-05T17:41:17Z**. Named symbols/counterexamples in the finding chapters are stronger anchors than line numbers.

The primary reviewer rebuilt this packet without launching agents. It used the compile-swarm-proposal skill, read the owner’s representation-first document and previous proposal, and traced source across core storage/query, native ownership, log lifecycle and TS generation. No test, typecheck, build, benchmark, credentialed backend operation, commit or push was performed by this reviewer. The still-running preceding Cursor task reports compiler checks in its own latest status; those reports are not accepted final qualification evidence. Document link/ownership/coverage checks do not qualify the product. No claim that every bug has been found.

Only final-solution Markdown is changed by this proposal pass. Existing code/test/skill/sibling work is preserved. The replaced packet is recoverable at **/tmp/bumbledb-swarm-repacket.OrFXAz/final-solution**; its latest in-flight status is also preserved as STATUS-latest-before-replacement.md in that directory’s parent. This is a temporary local recovery copy, not a durable release archive. Earlier source review copy /tmp/bumbledb-convergence-review.9H3qkR/tree remains historical best-effort evidence, not an atomic current revision.

Do not discard actual repairs because a finding was first observed earlier. Conversely, source symbols/new tests/status confidence do not close behavioral obligations. Reinspect moving source at F0 and retain acceptance cases even when a defect has been repaired.

## Source integration barrier — before any product check

1. Every mandatory producer, consumer and deletion in L01–L21 is implemented; no “future tightening” exception.
2. Coordinator has reviewed each lane’s source and all seven integration journeys in chapter 60. In particular: complete staging judgment; owned-generation/charge lifetime; one-worker session open; native multirow cursor ticket; coherent loss proof; checkpoint-only root; symbolic field backfill and kernel repository lock.
3. Public Rust/TS syntax, native declarations/exports, internal cross-package imports, formats, proof premises and examples agree. No stale writer ABI or unlimited/legacy twins.
4. Workers are quiesced before measurement. Shared hubs are reconciled and source scope is reviewed, without requiring interim compilation.
5. Permanent docs and executable tests now carry every needed semantic/API/performance contract, D01–D29, 68 audit IDs, 220 child behaviors and 78 prior-review obligations. Final executable inventory no longer derives from disposable Markdown.

A source barrier opens the final repair/qualification phase; it does not assert the code compiles. Compiler/type errors discovered there are repaired, with affected checks rerun. A required property cannot be dropped as a compiler fix.

## Correct retirement order

The qualification input must be the code that will actually be committed:

1. L21 transfers permanent contracts and the remaining qualification checklist to maintained docs; all lanes supply executable discriminators. Consolidate existing reference docs rather than mirroring two authorities.
2. Coordinator verifies no executable/doc link still depends on disposable packet chapters or deleted implementation reports. This includes obligation-inventory.generatedFrom, retirement docs, root PROMPT and release tooling.
3. Preserve remaining coordinator state outside the repo if resumption needs it, then retire **only** the reviewed final-solution files and root PROMPT.md. Preserve audit/ permanently and all independent semantic evidence. Do not delete the last checklist before its permanent replacement exists.
4. Capture the **post-retirement** candidate’s exact intended inputs. Begin final required checks against that candidate.
5. Repair failures without restoring shims or weakening gates; rerun all evidence affected by changed inputs. Missing required external environment is unqualified, not another speculative source wave.
6. Populate the existing final evidence index only from actual validated results. Do not create a placeholder Passed release-results.json.
7. Review and verify staged inputs match the qualified candidate, make the sole final integrated commit, verify its tree against the candidate, then push the authorized existing branch. Public release/package promotion/live migration remains separate authorization.

The earlier “review → checks → retire → commit” sequence is expressly withdrawn: retirement can change qualified inputs. Do not preserve it in docs/reference/final-solution-retirement.md.

## Final commands and evidence families

L21 must reconcile the current runner rather than invent another harness. The present scripts/battery.sh/check.sh, bridge build, packed-import and release-results tools are starting points, not authority to claim every environment passed.

Final execution order:

- Pinned Rust/TS formatting/lints/builds/type/declaration/doc/feature checks; compile workspace and excluded ts/crate against the same selected contract.
- One fresh addon build per exact candidate/platform before any addon-dependent test, with independently verified provenance if reused. Avoid package-test hooks rebuilding it a second time.
- Fast canonical/semantic/index/reference/collision/budget tests, D01–D29 composed discriminators, native ownership/Cause and independent protocol/failure/process tests.
- Current Lean/refinement and supported unsafe/Miri lanes; do not pretend unsupported substrate code ran under Miri.
- Fresh packed core/log installed outside workspace; addon-free pure imports separately; actual core/log application and Rust consumer journey using installed artifacts.
- Isolated authorized real filesystem/S3/IAM provider tests; streamed >RAM migration/recovery/backup/results, actual >32 GiB populated data and tenant churn.
- Controlled Apple Silicon, real Graviton Linux ARM64 and Linux x86-64 Node runtime/ABI/performance cells. No timing qualification while other workers load that host.
- Exact evidence completeness and final source/deletion review. Battery exit alone does not certify cells it never ran.

Do not dynamically install tools or contact paid/cloud services merely to conceal missing prerequisites. Use configured pinned tools and designated isolated test resources. If required credentials, hardware, disk or dependencies are unavailable, finish safe independent work and report the exact cell/prerequisite. Do not use a generic not-applicable waiver, create a fake report, endlessly spawn new waves or make an unauthorized interim “release” commit.

The post-promotion public-registry install cell PKG-07B is explicitly separate from prepublication qualification and requires owner-authorized promotion. It must be marked pending for that reason, not passed from a local tarball.

## Candidate identity without a commit-hash circle

Qualification precedes the sole final commit. Identify it with the existing recomputed candidateSourceDigest/spec revision, not old HEAD or a guessed future SHA.

Cover intended paths, file kinds, executable modes, bytes or symlink targets, additions/deletions, tests/build scripts/manifests/locks and permanent specs. Validate membership rather than trust a caller-supplied path list. Link identity hashes the link target, not arbitrary dereferenced content. Deleted tracked inputs have an explicit final-tree representation; they must not crash enumeration. Generated/ignored build inputs must be accounted for or proven non-input outputs.

Declare non-input evidence/output exclusions narrowly to avoid self-hashing result manifests; never exclude permanent specs or source to retain stale evidence. Bind actual tarballs/addons/data/reports, machine/backend facts, executed cases and skips to this identity. Reject stale, duplicate, missing, malformed or garbage evidence at audit, gate and target-cell levels. No unchecked digestOverride bypass.

The final staged/committed source must equal the qualified candidate inputs. An amended source file invalidates dependent results. If the implementation cannot establish that equality, it is not qualified.

## Handoff

A successful run leaves a smaller coherent database, current normal docs, purposeful tests, actual qualification evidence and one final reviewed commit/push. It does not leave this proposal forever or add an implementation/fixtures/exhaust hierarchy.

An incomplete run leaves an honest concise STATUS while the packet exists, or the transferred permanent checklist after retirement, with exact remaining ownership and prerequisites. Resume from current source, not a new architecture wave. Proposal quality is not proof of release readiness.
