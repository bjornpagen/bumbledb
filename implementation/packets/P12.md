# P12 — Adversarial integration and complete gate coverage

Packet / owner / phase: P12 / adversarial-integration agent (wave C,
2026-09-05) / F2 (no execution).

Verification: **NotRun** (F1/F2 rule: nothing here was compiled, typechecked,
linted or executed; every claim below is source-authored; F3 owns green).

## Files owned and changed

Created (all inside P12's exclusive prefixes; one writer per file):

- `implementation/verification-manifest.md` — **the F3 execution manifest**
  (chapter 64 deliverable): environment matrix, 18 local lanes with exact
  commands/runners/fixture sizes/expected nonzero inventories/artifact
  inputs/output paths, 9 external-blocked lanes each naming its precise
  missing authority, the instantiated F3 order, the nextest stdio LEAK
  investigation plan (no suppression/serialization), the complete 68-audit-ID
  map, the complete 220-child-family map (one row per child), the parent
  G00–G16 conjunction map, and the honest open coverage debts.
- `crates/bumbledb-log/tests/adversarial-process.rs` — REAL process
  pause/kill/restart schedules over the landed store+history (re-exec child
  harness, the `local_ownership.rs` pattern): SIGSTOP-suspended full-stack
  LocalHistory owner vs successor; SIGKILL exactly at the hosted publication
  boundary (head CAS landed via the FsStore `Phase::Published` park, local
  commit never ran) with reopen/catch-up/resolve; SIGKILL at the GC sweep's
  durable-progress CAS with resumed collection convergence; SIGKILL during
  fresh-cache hydration (owned staging polled on disk) with resumed complete
  hydration; the REC-05 mid-hydrate hold-revocation arm (authored
  2026-09-05, closing the packet's one recorded debt): a hydrate through
  `recovery::open_hosted` frozen in another process at a deterministic
  first-chunk-GET boundary (stdin-gated delegating store + SIGSTOP), the
  `HydrationHold` named root released via
  `admin::release_named_root_hosted` — the registered hold protects the
  closure through a full collection, the post-release collection reclaims
  it, the resumed hydrate refuses whole and typed (never a partial/wrong
  snapshot), the SIGKILLed variant adopts nothing, and both successors
  converge on the identical complete current state.
- `crates/bumbledb-log/tests/adversarial-migration.rs` — REAL kill schedules
  over `migration::executor` (P09 routed MIG-03/08 real-process arms here):
  kill mid-staging (polled on the targets namespace) and SIGSTOP+SIGKILL of a
  parked ReadyToSwitch holder; both resume to exactly one activated lineage
  with evidence-stable activation retries and no timer thaw.
- `crates/bumbledb-log/tests/adversarial-hostile-bytes.rs` — hostile REMOTE
  bytes against `apply::materialize` (the replay trust boundary): honest
  decision applies once and never twice; forged `Committed` for an unlawful
  command refuses whole; forged `InvariantRejected` for a lawful command
  refuses whole; foreign-identity decision refuses before evaluation; spliced
  parent refuses as a chain break; full truncation sweep + trailing-byte pad
  refuse with zero state movement.
- `crates/bumbledb-log/tests/adversarial-trace.rs` — the PRODUCTION machine
  driven through P11's adversarial schedules with
  `bumbledb_bench::closure::history_model` as the independent oracle and
  `verify_trace` as the shared checker (the model↔production bridge P11
  declared but could not wire): witnessed-decrement/ABA schedule merged into
  one trace under shared command ids; two-thread hosted contention with
  receipt/CAS trace lawfulness and duplicate-command single decision;
  lost-CAS-response (IndeterminateApplied) resolving to one stable outcome.
- `crates/bumbledb-log/tests/gate-baseline-ports.rs` — G00-rule ports of
  DELETED-API safety properties with audit citations: REP-020 all-or-none
  multi-relation command; SDK-001 later-command-preserves-earlier-resolution
  (IndeterminateDropped + attempts=1); REP-004/ENG-004 application-owned
  Id128 bytes, retry-stable, duplicate-follows-schema-law; ENG-007
  exhaustion-is-operational-never-semantic; REP-001/REP-007 stale-writer
  staging is an orphan, never history.
- `crates/bumbledb/tests/adversarial-close-drain.rs` — close/drain under load
  at the CORE boundary: close reports live leases (`CloseReport::Incomplete`)
  and never claims quiescence; retained leases read exactly during closing;
  drained close → typed refusals → real lock release → successor open;
  retained owned results survive close byte-for-byte; two writer threads
  racing close refuse typed only, and the reopened store passes the full
  offline sweep (`verify_store` → `StoreVerdict::Coherent`).
- `ts/test/adversarial-boundary.test.ts` — hostile capability inputs across
  the ACTUAL Rust/Node boundary: forged plain-object handles in every slot;
  kind confusion with REAL externals (directory owner in operation/session
  slots); one-shot take discipline; retained wrappers refuse after close with
  real release proven by a successor open over the same directory; close
  under load with settled callbacks; owned results frozen against teardown;
  stale prepared id + foreign relation id refuse typed inside a live session
  that survives the attacks.
- `ts-log/test/adversarial-capabilities.test.ts` — the PUBLIC log surface
  over the real runtime: same-schema cross-origin cache attack refuses before
  serving/mutating; retained ref resolves after reopen with the exact
  receipt; foreign-scope command is `not-submitted` pre-dispatch; closed
  capability refuses typed while an unrelated tenant is unaffected; open of a
  missing database never creates; a hosted binding under the local
  constructor refuses pre-dispatch. (RED until the wave-C `LogNative` verbs
  land — see dependencies.)

Rewritten (P12-owned per the ownership map):

- `scripts/release-results.test.mjs` — checker regressions extended: exact
  padded child-family spellings across every chapter-70 group (representative
  per family + unpadded-spelling refusal + audit/gate namespace
  disjointness); PKG-07B is the ONLY excused pre-promotion cell; every
  evidence field individually load-bearing (platform/toolchain/features/
  command/review/artifact/report/spec-revision); unknown/duplicate/missing
  rows each named exactly; skipped-with-reasons accepted, unexplained-skip
  refused.

Not touched: `scripts/release-results.mjs` (P00), `implementation/
release-results.json` (P00), every other packet's test files (read only),
all production code (defects recorded below instead of edited — the "never
edit production to make an oracle agree" rule).

## Input contracts received (IDs + actual symbols)

- C04 (P02, landed): `bumbledb::{Db, Error, ExecutionPolicy, WorkContext,
  Value, RelationId, Id128, FieldId, Admission, StoreVerdict}`,
  `bumbledb::store::{CloseReport, StoreError}`, `Db::{create, open, read,
  write, integration_store, verify_store}`, `WriteTx::{insert_dyn,
  delete_dyn}` — consumed as landed.
- C06/C07 (P04/P05, landed): `LocalHistory`, `HostedHistory` (+
  `with_attempts`), `SubmitOutcome`, `ResolveOutcome`, `LogError`,
  `Command::{seal, encode, command_ref}`, `CommandMetadata`, `Limits`,
  `UnverifiedOutcome`, `DecisionParts`/`encode_decision`,
  `apply::{materialize, ApplyError}`, `HeadAuthority::position`, the full
  coordinate roster; `store::{fence, fs::{FsStore, Phase, Inject},
  mem::{MemStore, Op, Behavior, Gate}, ObjectRef, ObjectKind, put_verified,
  get_verified, head_key}`; `manifest::{HeadRecord, GcPhase, TailPolicy,
  encode_head}`; `checkpointer::{publish_checkpoint, read_live_head,
  CheckpointKind, CheckpointPolicy}`; `gc::{close_epoch, mark, sweep,
  run_collection, GcPolicy}`; `recovery::{open_hosted,
  materialization_path}`; `codec::StreamLimits` — all consumed as landed.
- C11 (P09, landed): `migration::executor::{LocalMigration, MigrateOutcome,
  MigrationStatus, StepInput, SuffixRequest, ActivationRef, activate_target}`
  + the `migration_support` fixtures (schemas/plans/manifest) — consumed as
  landed via `#[path]` include (read-only reuse of P09's test-support module
  compiled into P12's binaries; the file itself is not edited).
- C09/C10 (P06/P07/P08, landed): `runtimeNative` roster
  (`ts/src/runtime-native.ts`), `lower`/`relation`/`schema`/`u64`/`str`
  authoring surface, ts-log `LocalHistory`/`Command`/identity constructors/
  outcome vocabulary — consumed as landed; the `LogNative` native verbs are a
  DECLARED roster only (dependency 2).
- P11 (landed): `bumbledb_bench::closure::history_model::{Model, Command,
  CommandId, Condition, StateStamp, Terminal, Event, verify_trace}` — the
  independent oracle and shared trace checker, consumed exactly as P11's
  packet hands them to P12.
- Test-harness reuse (read-only, compiled into P12 binaries): `crates/
  bumbledb-log/tests/lane_support/mod.rs` (P05's Mirror/fixtures) and
  `crates/bumbledb-log/tests/migration_support/mod.rs` (P09's fixtures).

## Output contracts supplied

- `implementation/verification-manifest.md` — the C-neutral F3 execution
  contract P00 runs and every packet's evidence routes through.
- The adversarial/gate suites above (inventory → IDs table below).
- Checker regressions guarding the ledger inventory/evidence rules.

## Implemented behavior (what is real)

Real process schedules (SIGSTOP/SIGKILL over re-exec children with piped
stdout markers and parent-side reaping), deterministic in-file kill points
via the landed FsStore Phase hooks, a stdin-gated delegating-store park for
the one deterministic READ boundary the mutation-only Phase hooks cannot pin
(the REC-05 mid-hydrate first-chunk GET), disk-polled kill points for staging
windows, race-tolerant safety assertions where a kill can land on either side
of a boundary (the property, not the schedule, is asserted), hostile-bytes
batteries over the replay trust boundary, a structural model↔production
differential through one merged event trace, and TS boundary attacks that a
typed refusal must survive. Every suite asserts zero state movement on
refusal, exact retained evidence on resume, and typed (never panicking,
never aliasing) failure surfaces.

## Deleted mechanisms

None (P12 adds evidence; deletions belong to subsystem packets). The prior
minimal `release-results.test.mjs` content is preserved and extended, not
weakened: every original assertion survives verbatim.

## Authored tests → audit/gate IDs (all NotRun)

| File | IDs |
| --- | --- |
| `adversarial-process.rs` (5 tests + child entry) | REP-003/005/009/010/013/017/019, SDK-001/006, OPS-005; PROTO-05/12, GC-03 shape/GC-04/12, REC-02/04/05 (the mid-hydrate hold-revocation arm), FS-01/02/04 full-stack halves, RUN-05; G06/G07/G10/G11 |
| `adversarial-migration.rs` (2 tests + child entry) | MIG-03/08/09/14 real-kill arms, TS-MIG-05/06 native halves, OPS-001; RUN-15; G10 |
| `adversarial-hostile-bytes.rs` (6 tests) | REP-008/018/020, ARCH-004, PROTO-03/15; E-CODEC replay half; G14 |
| `adversarial-trace.rs` (3 tests) | ASS-002 (the model↔production bridge), CONC-02/03/04, PROTO-01/02/04/08/09, ARCH-001/003, REP-006/015, SDK-001; G07 |
| `gate-baseline-ports.rs` (5 tests) | REP-001/004/007/020, ENG-004/007, SDK-001/015, PROTO-10/11/17; G00 (deleted-API safety ports) |
| `adversarial-close-drain.rs` (3 tests) | SDK-002/005/007 core halves, RUN-04 shape, E-SNAPSHOT/E-DURABILITY close rows, Q-LIFETIME retained results; G06/G11/G12 |
| `adversarial-boundary.test.ts` (6 tests) | FFI-01/02/05/07 attack halves, SDK-004/007, RUN-03/04/06 boundary halves, REP-016 (no live capability after close); G11/G14 |
| `adversarial-capabilities.test.ts` (5 tests) | SDK-016, ARCH-004, REC-03 TS half, RUN-06, PROTO-02/17 client side, OPS-005 ("open never creates"); G14 |
| `release-results.test.mjs` (8 tests) | G00/G16 evidence machinery, ASS-004 |

Coverage routing for ALL 68 audit IDs and ALL 220 children is the
verification manifest (sections 6–7), including the external-blocked lanes
with named missing authorities.

## Independent review notes per packet (source read, not only diffs)

- **P01** — Landed roster matches its packet claims (fresh fully
  unrepresentable, judge sees the multimap, v6 fingerprint byte goldens).
  Open item P12 tracks: the canonical `Violations` evidence codec is still
  Display-bytes inside P04's `decide::judge_and_seal`; rejection evidence in
  decision objects/backups is therefore provisional (C12) — my
  `adversarial-hostile-bytes.rs` deliberately never pins evidence bytes.
  Concern seconded from P11: `float/cast.rs` host-expectation tests should be
  paired with oracle bits at F3.
- **P02** — The close/drain/lock semantics my `adversarial-close-drain.rs`
  exercises are exactly as declared (gate drain, Incomplete report, lock
  released on drop). `Error::Store(Box<StoreError>)` landed as requested.
  Noted (cost, not defect): keyed point reads and `UnindexedRows` remain
  reference walks — the recorded C04/C05 acceleration follow-up; P14's
  post-write cells will surface it.
- **P03** — The one honest gap that limits gate coverage is the UNWIRED
  sink-internal spill (their own packet records it): Q-BUDGET/Q-DISK hold for
  inputs/outputs but not intermediate distinct/group/rec state. Routed as
  manifest debt 1; P03R (wave C) owns the wiring. Also seconding P07's
  request that `ir::render` quote id128 literals to preserve render-reparse.
- **P04** — `writer/hosted.rs` still publishes BARE control head bodies and
  stages decisions under the constructor-time epoch; P05's exact patches are
  routed to P04R. My process/publication tests are deliberately
  layout-agnostic (they drive HostedHistory against itself), so they survive
  the patch; the GC/recovery arms use the lane-support Mirror (composed
  HeadRecord layout) and will only integrate with HostedHistory-created
  tenants AFTER P04R lands. `blank_initial_digests` mismatch (P05 defect 2)
  must land before L14 golden freezing — flagged in the manifest order.
  Review positive: admission guard ordering matches P11's model exactly, and
  `apply::materialize` re-judging + recorded-outcome conviction held up
  against every forgery class I authored.
- **P05** — The deepest lane inventory in the tree; my packet adopted its
  recorded F3 gaps verbatim (S3/power/large lanes → X01/X05/X02). On P05's
  open review question (is the Mirror too close to production framing to be
  an oracle?): my resolution is that the Mirror is a HARNESS, not the oracle
  — the independent oracle for history semantics is P11's model, and
  `adversarial-trace.rs` wires it against production directly; the Mirror's
  digest cross-check stands as a framing regression, not semantic evidence.
  Recorded as the answer to P05.md's cross-review request.
- **P06** — The reactor's no-unsafe-Send claim is structurally verifiable
  from the session file (only owned data crosses the channel); the
  double-release/stale-id/foreign-runtime arms exist Rust-side; my TS
  boundary file adds the forged-external/kind-confusion attacks from the JS
  side that the Rust tests cannot spell. Note: `runtimeTake`'s double-take
  behavior is not pinned in P06's docs; my test accepts null-or-typed-throw
  but a single delivered value — P06R2 should pin one spelling at F3.
- **P07** — Effect-surface pins are the right shape (type-level twins
  absence). The one-shot-iterator rerun semantics decision (reads empty, does
  not refuse) matches chapter 35's text. Computed find terms remain
  unspellable from TS (their boundary 2) — manifest debt 3.
- **P08** — The certainty layer is careful (E=never submit, pre-dispatch
  refs, Cause preserved). One reviewed judgment call: a FORGED command
  capability is an `Effect.die` defect rather than a typed failure — correct
  under chapter 35's programmer-error doctrine, but the packed-consumer docs
  (P13) should state it. My `adversarial-capabilities.test.ts` intentionally
  drives only authentic-but-misdirected capabilities (typed refusals), and
  the roster test remains the forcing function for the native verbs.
- **P09** — The resume-pass defect fixes (pre-freeze evidence resolution;
  `PlanSetMismatch` vs `SourceFrozenByOther`) are exactly the races my kill
  arms exercise; `resume_and_activate` in `adversarial-migration.rs` asserts
  the corrected semantics. `initialize` refusing an empty suffix and the
  `b'm'`-vs-`b'r'` key-namespace separation both held up under source read.
- **P10** — The leftover-removal rule ("only files the manifest does not
  name, matching exactly the next unrecorded sequence") is sound given the
  manifest-last commit; `check` never removing anything is the right
  asymmetry. Stale-intent refusal ergonomics must reach P13's example docs
  (their dependency 6) — routed in the manifest's X06 row.
- **P11** — `verify_trace` checks exactly two invariants (one CAS winner per
  revision; one stable outcome per id); read-freshness/witness observations
  (PROTO-05/16 read side) are NOT trace-checkable through it and are asserted
  directly in the production suites — a scope note, not a defect. The model's
  Deleted-before-lookup rule got its production twin in P04's guards
  (verified by source read). Corpus regeneration (RNG stream shift) is
  in L14 with the "ALL theorygen corpora" warning preserved.
- **P13** — Not landed at review time (wave C concurrent). Reviewed its 62
  scope; the manifest routes every PKG/APP child it owns and marks the
  deployment/registry lanes as X06/X07/X08 with named authorities so its
  landing cannot silently narrow them.
- **P14** — The NOSYNC-lane deletion (decision 1) is the correct ENG-008
  consequence and my manifest's L16 order preserves "durable first, no-sync
  reported separately only if it exists" as: it does not exist. The KAT-file
  hand-copy is a hard F3 precondition (absence = NotRun) — carried into L16's
  inventory rule. `PostingMint::probe` O(n) setup cost is acceptable and
  recorded by them.
- **P00 hub state** — campaign-status wave-B hub patches match the landed
  source everywhere I read (integration re-point, Store error family, log
  lib.rs roster, barrels). One stale row: campaign-status still lists
  `braids.rs (delete)`/`vector.rs (delete)` in P04's ownership line — purely
  historical, no action needed.

## Unresolved source dependencies and known defects

1. **HUB (blocking compile of `adversarial-trace.rs`):** `bumbledb-bench`
   dev-dependency in `crates/bumbledb-log/Cargo.toml` — patch request 1.
2. **Native log verbs (P06R2/P05/P09 over P08's `LogNative` roster):**
   `adversarial-capabilities.test.ts` drives the production TS surface and is
   RED until they land (as is P08's `native-roster.test.ts`). Expected
   mid-cutover; owed green at F3.
3. **P04R head-frame patch:** after it lands, `adversarial-process.rs`'s
   publication-kill arm still compiles/behaves (it never decodes head bytes);
   if `HostedHistory::create`'s signature changes (it should not — TailPolicy
   was requested as a field default), this file's constructor calls follow.
4. **TS `runtimeTake` double-take spelling** (P06R2): my boundary test
   accepts null-or-typed-throw; pin one behavior.
5. **REC-05 process arm** — CLOSED 2026-09-05: authored as
   `adversarial-process.rs::a_hold_revoked_mid_hydrate_*` (child mode
   `hydrate-hold-revoked`); manifest §8.2 updated to closed-authored and the
   §7 REC-05 row carries the anchor. Verification: NotRun (execution is
   F3's, lane L04).
6. All suites are authored against the F1/F2 tree and NEVER run; ordinary
   integration repair at F3 is expected under the chaos rule (e.g. exact
   error-variant spellings in `matches!` arms).

## Hub patch requests (P00 applies)

1. `crates/bumbledb-log/Cargo.toml` — add to `[dev-dependencies]`:

   ```toml
   bumbledb-bench = { path = "../bumbledb-bench" }
   ```

   (Needed by `tests/adversarial-trace.rs` for the P11 independent model +
   `verify_trace`, exactly as chapter 62 routes it. Acyclic: bumbledb-bench
   depends on bumbledb only, never on bumbledb-log — its model independence
   is the point. Cost: rusqlite(bundled)/aegis/blake3 join the log crate's
   DEV graph only.)
2. F3 checklist adoption: `implementation/verification-manifest.md` §4 is the
   execution order; §3's X-lane authorities are the ask list for the owner
   (S3 scope, storage runner, Linux cgroup host, Graviton/x86 runners, power
   rig, cloud scope, registry scope).
3. Notify P06R2: pin `runtimeTake` double-take behavior (dependency 4) and
   keep `HostedHistory::create`'s signature stable through the P04R patch
   (dependency 3).
4. No scripts/ change requested: `battery.sh` already runs
   `scripts/release-results.test.mjs` first; the new Rust/TS files ride the
   existing workspace/package commands (hyphen-prefixed integration tests are
   ordinary cargo targets; `test/**/*.test.ts` globs match the new TS files).

## Cross-review challenge / resolution

- Challenge (self): are the race-tolerant kill assertions ("either side of
  the boundary") too weak? Resolution: each arm asserts the SAFETY property
  that must hold under every schedule (no thaw, no torn head, one lineage,
  complete-or-absent hydration) plus at least one deterministic kill point
  (marker-parked child inside a Phase hook); determinism where the landed
  hooks allow it, tolerance only where the kill window is disk-polled.
- Challenge (self): does reusing `lane_support`/`migration_support` violate
  test independence? Resolution: they are fixtures/harnesses (schema
  builders, the mirror publisher), not oracles; every oracle in P12's files
  is either the independent P11 model, an explicit expected value, or a
  refusal-plus-zero-state-movement assertion.
- Answered P05's open review question (mirror-as-oracle) — see the P05
  review note above.
- Open for P00/P04R/P06R2 review: dependencies 2–4.

Verification: **NotRun** (F2; execution is F3's, per the manifest).
