# Packet P06 — Native scheduling, ownership, affinity and the private Node bridge

Packet / owner / phase: **P06 / native-runtime lane / F1–F2 (wave-C resume,
P06R2: the declared-but-missing native verb rosters; wave-E close pass
2026-09-05: the wave-D P08 native-bridge defects — see "Wave-E close pass"
below; wave-E F2 closure 2026-09-05: P04.md hub request 5 — per-call submit
options + read-side catch-up wiring, defects 4/8 resolved — see "Wave-E F2
closure" below).**

Verification: **NotRun** (F1; no test/typecheck/build/probe executed by this
packet — every green claim below is source-authored and F3 owns it).

## Files owned and changed

Exclusive write paths (per campaign-status.md): the whole `ts/crate/src/`
directory (incl. `lib.rs`), `ts/src/{native.ts, runtime.ts, runtime-native.ts,
runtime-codes.ts, runtime-errors.ts, marshal.ts}`, and
`crates/bumbledb-log/src/tenants.rs`.

State across the whole P06 effort (wave-A managed-owner cutover, wave-B
reactor, THIS wave-C verb-roster completion). Files as they now stand:

- `ts/crate/src/db_wire.rs` — **NEW (this pass): the complete core db-bridge
  verb roster `ts/src/db-native.ts` declares** (C09/C05): charged schema
  compile (`runtime_schema_compile`/`runtime_schema_take`), coherent owned
  snapshots over the reactor (`runtime_db_snapshot`/`runtime_snapshot_take`
  with the `{store, generation}` CoreWitness wire, `runtime_snapshot_close`),
  snapshot-bound execution sessions (`runtime_snapshot_session` →
  `runtime_session_take`; a spendable capability SHARING the snapshot's
  pinned thread/generation), point reads (`runtime_snapshot_get` →
  `runtime_row_take`), complete bounded execution into sealed
  `bumbledb::CompleteResult`s (`runtime_snapshot_execute` /
  `runtime_session_execute` → `runtime_result_take`), bounded collect that
  refuses BEFORE materializing past the caller's resultBytes and leaves the
  sealed backing (`runtime_result_collect` → `runtime_rows_take`), the
  one-shot atomic cursor transfer + byte-bounded page pulls with a buffered
  overflow row and honest terminal EOF (`runtime_result_cursor`/
  `runtime_cursor_take`/`runtime_cursor_next`/`runtime_page_take`/
  `runtime_cursor_close`), database-free change drafts with a CUMULATIVE
  cross-chunk input budget that spends the draft on failure
  (`runtime_draft_open/insert/delete/finish/close`, `runtime_report_take`,
  `runtime_changes_take`, `runtime_changes_close`), one immutable
  final-state apply over the integration writer with witness comparison as
  a DOMAIN outcome and single-writer admission by refusal
  (`runtime_db_apply`/`runtime_apply_take`), bounded inspection
  (`runtime_db_inspect`/`runtime_db_inspect_take`), the shared canonical
  row codec as the ChangeSet grammar (`runtime_encode_rows`/
  `runtime_bytes_take`/`runtime_decode_rows`), and the two read-only
  migration-codec entrypoints (`runtime_migration_schema`/
  `runtime_migration_read`). Retained resources (results/cursors/drafts/
  change sets) are counted against `native_handle_capacity` and
  byte-charged against the resultBytes aggregate while retained
  (`RetainedNative`); closes are join-idempotent (`spawn_teardown` — the
  contended path drains through a self-reclaiming registered teardown job,
  never a blocked JS thread, and reports `Failed` honestly when teardown
  could not run). Compile locks assert `CompleteResult`/`ResultCursor` are
  `Send` — if P03R makes them `!Send` the build breaks HERE, never an
  `unsafe impl`. Engine-backed tests in `db_wire/tests.rs`.
- `ts/crate/src/log_wire.rs` + `log_wire/admin.rs` — **NEW (this pass): the
  complete LogNative roster `ts-log/src/native.ts` declares (C10)** over the
  landed P04/P05/P09 surfaces: `log_error_codes()` pinned to
  `ts-log/src/codes.ts` EXACTLY (**33 codes at the wave-E close pass** —
  `MaterializationStale` inserted directly after `MaintenanceRequired`; same
  order — a Rust test include_str!s the TS file and compares, now also
  pinning count 33 and the adjacency); history open/create
  (`log_history_open`/`log_history_take`) running P05's recovery machine
  (`recovery::{open_local, create_local, open_hosted}`) INSIDE one
  registered job with the kernel fence installed into the runtime's own
  owner registry (`reserve_owner_slot`/`install_owner_lock`), Local and
  Hosted (S3Store) backends, checked creation artifacts (the canonical
  `schema_file::render` snapshot must fingerprint to the created identity —
  empty/foreign artifacts refuse `UnsupportedArtifact`), idempotent
  creation retries (matching identity completes; strangers refuse
  `AuthorityExists`), `ForeignIdentity`/`WrongLineage`/`DatabaseMissing`/
  `CacheIdentityMismatch` refusals, and the explicit
  `discardMismatchedCache` quarantine (rename aside, never delete, never
  submit); history calls (`log_history_call`/`log_history_result`):
  submit (certainty union verbatim — Decided/NotSubmitted/OutcomeUnknown
  with the typed `{source, reason}` frame as DATA), resolve, bounded
  inspect (authority + hosted head tail/roots/gc + map report + registry
  op counts), and published snapshots that pin one coherent generation on
  the reactor AND capture the authority attachment INSIDE the pinned frame
  so provenance is exactly the pinned data's (freshness cached/latest/
  at-least with typed `NotYetAvailable`/`WrongLineage` refusals; the
  snapshot wire hands out BOTH the log handle and a
  `db_wire::SnapshotHandle` — the core reader capability is literally the
  same native resource, so `internalPublishedReader`'s verbs re-judge it);
  command lanes (`log_command_seal` deriving the runtime FROM the
  registered core ChangesHandle per the P08 resume signature — no runtime
  argument; native schema re-judgment; the declared-result record codec is
  the ONE core authority `bumbledb::canonical::result` since the wave-E
  close pass (the local byte twin was the recorded C12 defect — deleted);
  `log_command_decode`/`log_command_take`/
  `log_command_encode`/`log_bytes_take`/`log_command_close`); the
  native-backed typed tenant cache (`log_cache_make/take/acquire/
  log_borrow_take/inspect/evict/close`, `log_borrow_release`) over MY
  `TenantRegistry` — distinct one-shot borrows, per-call counted operation
  leases (a borrow's in-flight history call holds an `OperationLease`
  guard so eviction/close cannot tear the owner down underneath it),
  evict-borrowed refusing `SlotBorrowed`, capacity pressure evicting
  least-recent idle only, `RuntimeExpectation` enforcement (expected
  schemaId + appliedPrefixDigest recomputed from the ACTUAL chain records
  via the public manifest prefix fns — `MigrationRequired` on mismatch);
  and the one `logAdmin` family (`log_admin`/`log_admin_take`) with the
  certainty envelope decided NATIVELY: completed/report on success,
  not-started for provably-undispatched refusals, outcome-unknown for
  dispatched-but-unproven hosted outcomes. Verbs: checkpoint
  (hosted `publish_checkpoint`; local → typed Misuse: LMDB is complete,
  pin-root is the local specialization), pin/release named roots (local
  `local_roots`, hosted `add/release_named_root_hosted`), rotate/retire
  receipt epochs (hosted retirement rides `CheckpointKind::RetireReceipts`
  then `apply_hosted_retirement_locally`), collect-garbage
  (`gc::run_collection`; stopped passes report outcome-unknown with
  durable progress retained), backup (`backup_root` from the hosted head's
  recovery root into an Fs/S3 destination; local → typed Misuse naming
  pin-root as the artifact), verify-backup (read-only `Report`), restore
  (`restore_writable_with_tail` into a NEW local incarnation from the
  backup's checkpoint + exact tail), erase (local tombstone via
  `erase_local`; hosted `erase_hosted` with retainRoots complemented into
  release_roots and the honest residual inventory), and the migration
  workflow over P09's executor: status (up-to-date with the recomputed
  applied prefix digest / pending labels / in-progress / aborted with
  full `MigrationRefWire` refs incl. the freeze intent's plan-set digest),
  initialize (execute the generated chain from the declared empty base —
  seeds run exactly once — activate, and install the activated target as
  the tenant's materialization), migrate (verify → suffix at the applied
  prefix → ONE staged target → ReadyToSwitch with the deployment binding;
  a post-freeze failure reports completed(paused) with the frozen source
  — never a silent thaw), activate (`activate_target` under the stable
  namespace lock; matching retries are evidence), abort
  (`LocalMigration::abort` — fence FIRST, thaw only the exact matching
  operation). Hosted migration execution refuses typed
  `MigrationUnsupported` (recorded C08 boundary: the staged hosted data
  plane is P05's). The planned target incarnation is derived
  deterministically from the operation id (domain-prefixed engine hash;
  provisional until C12) so a stable ref exists BEFORE dispatch and a
  retry resumes the same target. Engine-backed tests in `log_wire/tests.rs`.
- `ts/crate/src/migration_wire.rs` — **NEW (this pass):** the two read-only
  C11 responses (`runtimeMigrationSchema` → `{schemaId, snapshot}`;
  `runtimeMigrationRead` `kind:"chain"` → headPrefixDigest /
  planSetDigest / appended{entry, planText, manifestText}) with P10's exact
  request/response grammar. All SEMANTIC work is native
  (`schema_file::{schema_id, render}`, `migration::{plan, manifest}`); this
  file is envelope transport only — a minimal strict JSON reader/renderer
  (objects/arrays/strings/booleans/null/non-negative-integer numbers,
  bounded depth, duplicate keys/floats/negatives/leading zeros/trailing
  bytes refuse) re-renders embedded plan/manifest subtrees to text for the
  native parsers. Refusal envelope `{refused: {code, detail}}` with
  `MigrationDrift`/`MigrationUnsupported`/`Misuse` codes. Unit tests
  in-module (strict grammar, escape round-trips, fresh-chain base prefix
  agreement with the native digests).
- `ts/crate/src/runtime.rs` — `Output` gained the db/log arms (Descriptor,
  ExecSession, CompleteResult, ResultCursor, Page, Draft, Changes, Apply,
  DbReport, Bytes, Machine); the FS arm and `runtime/fs.rs` are DELETED
  (see deletions); the post-work cancellation carve-out generalized to
  `Output::mutation_evidence()` (write conclusions, applies, decided
  submits and completed admin values are dispatched-mutation EVIDENCE —
  interruption may discard delivery, never rewrite them into rollback
  claims); `submit_db` (operations bound to a managed database for
  busy/teardown accounting); `database_operations` (bounded diagnostics);
  `retain_native`/`RetainedNative{grow}` — retained bridge resources
  counted against `native_handle_capacity` and byte-charged against the
  resultBytes aggregate until release; `State.natives` counter.
- `ts/crate/src/runtime/session.rs` — `SessionOpened` gained `store`
  (persisted store identity hex — the CoreWitness wire's store half) and
  `attachment` (the committed authority control read INSIDE the pinned
  frame — the published snapshot's provenance basis);
  `spawn_read_session_for` (the db/log bridge entry to the reactor from
  inside a registered job over an already-held lease). Reactor mechanics
  and the wave-B test module unchanged.
- `ts/crate/src/runtime/owners.rs` — `reserve_owner_slot`/
  `install_owner_lock`/`abandon_owner_slot` (the log machine's history
  opens run the fence acquisition inside their own registered job and
  install the held lock into the one registry — never a second ownership
  authority); `lease_database_at` (admin verbs reuse an already-open
  tenant instead of double-acquiring its fence); `DbLease::runtime()`.
- `ts/crate/src/runtime_wire.rs` — `SessionHandle` now carries
  `Arc<SnapshotSession>` plus an `exec` spend flag: the owner capability
  (close drains the pinned thread) and the snapshot-bound EXECUTION
  session (close spends only the capability) are ONE Rust type, so
  `runtime_session_close` serves both and a spent exec session refuses
  `ClosedHandle` before dispatch; `runtime_session_take` returns
  `Either<SessionOpenedWire, External<SessionHandle>>` (worker open vs
  exec session); the prepared-id execution verb is RENAMED
  `runtime_session_execute_prepared` (the db-bridge's
  `runtime_session_execute` is the ParsedQuery form — resolves the P07
  name collision); `runtime_fs`/`runtime_fs_take` DELETED; the managed
  create path now surfaces `Error::DestinationExists` as the
  `destinationExists` refused kind (chapter 30: create refuses existing
  authority as a domain refusal, never Io) and every refused kind rides
  the one `tags::open_kind` table; `reason_object` extracted from
  `thrown` (the log frame nests the same core reason objects);
  `addon_identity`/`operation_runtime`/`reporter` shared with the sibling
  wire modules.
- `ts/crate/src/tags.rs` — `find_term` and `head_term` tables are now
  exhaustive over P03R's landed `FindTerm::Compute(ScalarExpr)` /
  `HeadTerm::Compute` (the recorded seam); NEW `scalar_expr` and
  `numeric_cast` tables (the computed-find expression lane, spelled
  exactly as the migration plan JSON grammar spells the same roster);
  `open_kind` gained `destinationExists` (seam resolved: the bridge and
  `ts/src/native.ts` now speak the same 4-tag roster). The `tags.json`
  golden test gained the two new tables — the FIXTURE regeneration is
  P07's (hub request below).
- `ts/crate/src/marshal.rs` — `scalar_expr_in` (bounded-depth computed-find
  expression parser over the frozen core roster; `var` binds a rule
  variable ordinal), compute arms in `find_term_in`/`head_term_in`;
  interiors now parse FULL `Rule`s (P03's generalized
  `Interior { rules: Vec<Rule> }` — aggregate/computed interior heads are
  legal; the old ProjectionRule narrowing was a stale compile break);
  `schema_value_in` exposed crate-wide (the draft/codec lanes reuse the
  one cell parser).
- `ts/crate/src/lib.rs` — module roster gained db_wire/log_wire/
  migration_wire; `seal`/`hex_fingerprint` crate-visible. Managed owner
  machinery unchanged.
- `ts/crate/src/log.rs`, `fingerprint_lock.rs` — unchanged this pass (the
  grammar lanes remain; the MACHINE lanes are log_wire's).
- `ts/src/native.ts` — `FindTermIr` gained the `compute` arm and the
  `ScalarExprIr`/`NumericCastIr` roster (C05 computed find terms are now
  SPELLABLE on the wire — P07's recorded boundary 2 unblocks);
  `HeadTermIr` gained `compute`.
- `ts/src/runtime-native.ts` — `runtimeSessionExecute` renamed
  `runtimeSessionExecutePrepared` (prepared-id form; the ParsedQuery form
  is db-native.ts's `runtimeSessionExecute`); `runtimeFs`/`runtimeFsTake`
  + FsRequestWire/FsResultWire DELETED (the 0.x five-verb JS transport
  died with P05's store rewrite — no JS layer holds a conditional-store
  verb).
- `ts/src/runtime.ts` — the payload-less native CloseWire "failed" now
  decodes as the core `Internal` reason (the chapter-35 CloseReport
  policy; matches the ts-log bridge so one wire arm has one meaning —
  the recorded QueueFull-vs-Internal seam is resolved). Everything else
  unchanged.
- `ts/src/runtime-codes.ts`, `ts/src/runtime-errors.ts`,
  `ts/src/marshal.ts` — unchanged this pass.
- `crates/bumbledb-log/src/tenants.rs` — gained `counts(binding)` and
  `report()` (+`SlotReport`): bounded bookkeeping reads for the cache's
  evict/inspect verbs (never a teardown decision by themselves). The
  registry semantics and its wave-B test module are unchanged.

## Input contracts received (IDs + actual symbols)

- **C04 (P02/P02R) — consumed.** As before, plus `Db::integration_writer`/
  `WriterSession::{generation, prepare}`/`PreparedWrite::seal`/
  `SealedWrite::commit` (the apply lane), `Store::{identity,
  map_report}`/`StoreIdentity`/`MapReport` (witness store hex +
  inspection), `ReadInstance::integration_host_attachment` (published
  snapshot provenance inside the pinned frame).
- **C01 (P01):** `ChangeSet{builder, parse, as_bytes, schema, len}`,
  `ChangeSetBuilder{insert, delete, finish}` (drafts + the row codec),
  `canonical` row grammar, `ScalarExpr`/`NumericCast` (marshal),
  `SchemaFingerprint`, `Id128`.
- **C05 (P03/P03R) — consumed as landed.** `FindTerm::Compute(ScalarExpr)`
  + `HeadTerm::Compute` (tags/marshal exhaustive), `Interior{rules:
  Vec<Rule>}`, `PreparedQuery::execute_complete` → `CompleteResult` /
  `ResultCursor{next_page}` / `ResultPage{rows, terminal}` per P03.md's
  seam note ("hold it on the owning worker like any other !Send resource"
  — resolved: both types are structurally `Send`, compile-locked in
  db_wire.rs, so results detach from the session and NO session
  paged-take verb is needed; the deliverable-4 decision).
  THREE small visibility/API requests to P03R below.
- **C06/C07 (P04/P04R) — consumed.** `LocalHistory{create, open, submit,
  resolve, authority, identity, db}`, `HostedHistory{create, open, submit,
  resolve, identity, db}` (the landed head-record composition — P04R's
  P05 patches are in), `SubmitOutcome`/`ResolveOutcome`/`LocalHealth`,
  `Command{seal, parse, encode, command_ref, changes, result, metadata}`,
  authority/receipt/decision rosters, `verbs::ConditionalStore` (+ the
  `&T`/`Arc<T>` delegation — the bridge shares one `Arc<S3Store>` between
  `HostedHistory` and its own head reads).
- **C08 (P05) — consumed.** `recovery::{open_local, create_local,
  open_hosted, materialization_path, write_binding, OriginBinding,
  RecoveryError, RecoveryRefusal, Recovered}`, `store::fence`,
  `store::{fs::FsStore, s3::{S3Store, S3Config, S3Credentials}}`,
  `checkpointer::{publish_checkpoint, CheckpointKind, CheckpointPolicy,
  read_live_head}`, `gc::{run_collection, GcPolicy}`,
  `admin::{local_authority, local_state, rotate_receipts_local/hosted,
  retire_receipts_local, apply_hosted_retirement_locally,
  add/release_named_root_hosted}`, `local_roots::{create_restore_point,
  release_restore_point, clean_roots}`, `backup::{backup_root,
  verify_backup, read_backup_manifest, read_backup_tail}`,
  `restore::restore_writable_with_tail`, `erase::{erase_local,
  erase_hosted}`, `manifest::{HeadRecord, RecoveryRoot, NamedRoot,
  RootKind, RootPolicy, GcPhase, TailPolicy}`, `codec::{StreamLimits,
  decode_manifest}`, `store::get_verified`.
- **C11 (P09/P10) — consumed.** `schema_file::{parse, render, schema_id}`,
  `migration::plan::{parse_plan, render_plan}`,
  `migration::manifest::{parse_manifest, render_manifest, verify_manifest,
  bind_plans, plan_set_digest, append_entry, prefix_at,
  base_prefix_digest, next_prefix_digest}`,
  `migration::executor::{LocalMigration, SuffixRequest, StepInput,
  ActivationRef, AbortRequest, MigrationStatus, MigrateOutcome,
  activate_target, initialize}`, `migration::history::{HistoryRecord,
  decode_record, history_key}` (applied-prefix recomputation for
  `RuntimeExpectation`). P10's exact ChainRequest/ChainPayload grammar
  (ts-log/src/migrations/codec.ts) implemented verbatim.
- **C10 (P08) — the wire roster implemented verbatim** (names, unions,
  `{source, reason}` error frame, join-idempotent closes,
  `logErrorCodes()` == ts-log/src/codes.ts), including the RESUME
  signature `logCommandSeal(change, policy, request, callback)` (runtime
  derived from the registered change handle). Wire GAPS discovered while
  implementing are hub-requested below (schema/snapshots/backup fields).

## Output contracts supplied — C09 (updated actual symbols)

Everything from the wave-B handoff stands (runtime lifecycle, directory
owner + managed DB open, worker-affine sessions/writers, builder admission,
managed publish, grammar lanes) MINUS the deleted `runtime_fs` transport,
PLUS:

- The complete db-bridge roster of `ts/src/db-native.ts` (exact names).
- The complete LogNative roster of `ts-log/src/native.ts` (exact names) +
  `log_error_codes()`.
- The two migration-codec entrypoints of `ts/src/migration.ts`.
- `runtime_session_execute_prepared` (renamed; runtime-native.ts updated).
- Every new operation is registered, charged, cancellable via
  `runtime_cancel`, and drained at close/shutdown; every retained native
  resource is handle-counted and byte-charged while retained; every close
  verb is join-idempotent and never blocks the JS thread.

## Deleted mechanisms (this pass)

- The 0.x five-verb JS filesystem transport: `runtime_fs`/`runtime_fs_take`
  (runtime_wire.rs), `ts/crate/src/runtime/fs.rs` (whole file), the
  `Output::Fs` arm and its bespoke cancellation carve-out, and the
  `runtimeFs`/`runtimeFsTake` + FsRequestWire/FsResultWire declarations in
  runtime-native.ts. All object-store work is native (C07); no JS layer
  holds a conditional-store verb. (The TS side was already deleted by P08;
  this removes the dead native half.)
- The stale ProjectionRule narrowing of query interiors in marshal.rs
  (P03's generalized full-Rule interiors are the landed shape).
- The payload-less "failed" drain's QueueFull mis-decoding in runtime.ts.

## Recorded-seam resolutions (this dispatch's item 3)

1. **OpenKind destinationExists** — ADOPTED into the bridge:
   `tags::open_kind` carries it, both managed create and publish emit it,
   `ts/src/native.ts` already declared it. P07's `tags.json` fixture and
   the relaxed wire-tags pin can retighten (hub request 4).
2. **CloseWire "failed" mapping** — core runtime.ts now decodes it as
   `Internal` (matching the ts-log bridge; chapter 35: a failed drain is
   bounded diagnostic data, never retryable submit backpressure).
3. **wire_tags exhaustive over Compute** — done (find_term + head_term +
   the new scalar_expr/numeric_cast tables + marshal parsers + the TS
   `ScalarExprIr` wire in native.ts).
4. **C05 paged-take-verb decision** — NOT needed: `CompleteResult`/
   `ResultCursor` are owned `Send` values (compile-locked), so results are
   registry-owned resources accessed by ordinary pool jobs; pages are
   byte-bounded per pull in the bridge over `into_cursor(1)`.

## Wave-E close pass (2026-09-05): the wave-D P08 native-bridge defects

Closed against P08.md hub requests 7–8, P04.md wave-C hub request 3, and
P01.md's C01 CommandResult addition. Files changed: `ts/crate/src/log_wire.rs`,
`ts/crate/src/log_wire/tests.rs`, `ts/crate/src/runtime_wire.rs` (doc pin),
`ts/crate/src/runtime/tests.rs` (pin comment). Verification: NotRun.

1. **P08 hub request 7 DISCHARGED — one result-record codec.** The local
   twin in `log_wire.rs` (`RESULT_FAMILY`/`RESULT_LAYOUT`/`ResultCell` and
   the hand-rolled encode/decode: u64 count, u64 name lengths, NO kind
   byte, tag 8 refused as Corruption) claimed family `bumbledb.result.v1\0`
   layout 1 with DIFFERENT bytes than the core authority
   `crates/bumbledb/src/canonical/result.rs` (kind byte + u32 count + u16
   name lengths + tag 8 Id128). The command digest covers these bytes, so
   the twin was a real C12 defect: it is DELETED, and
   `encode_result_record`/`decode_result_record` are now thin wrappers over
   `bumbledb::canonical::result::{encode_result, decode_result}` at
   `LIMITS.result_bytes`, threading the job's `WorkContext` (the seal call
   site passes its registered context). The JS-value halves keep the
   TS-confirmed CommandScalar mapping verbatim (`result_cell_in` now
   produces core `Value` scalars: boolean→0 Bool, bigint sign-split→1 U64 /
   2 I64 with out-of-range refused — the TS side ALSO refuses widths
   pre-dispatch, string→4 Text, number→3 canonical F64, Uint8Array→5
   Bytes; TS never emits tag 8 — a 32-hex string stays tag 4, no string
   sniffing). An Id128 cell (tag 8, Rust-sealed commands) now DECODES and
   crosses to JS as its canonical 32-lowercase-hex text per the recorded
   wave-D decision (re-sealing from TS respells it as tag 4: a NEW command
   with its own digest). Codec-error mapping: encode-side caller refusals
   (non-scalar/name/duplicate/budget) → protocol `Misuse`; strict-decode
   refusals → `Corruption`; `Work` stays the core Work reason. Receipt
   rendering keeps the surfaced-empty policy for a malformed stored record
   (the receipt remains intact evidence) and charges the decode against a
   bounded LOCAL work budget (`result_take_work`, sized by
   `LIMITS.result_bytes`) because take-functions run outside any registered
   job — a bounded decision recorded here.
2. **P08 hub request 8 / P04 hub request 3 DISCHARGED — roster row + frame
   arms.** `PROTOCOL_CODES` gained `"MaterializationStale"` directly after
   `"MaintenanceRequired"` (**33 rows**, byte-identical to
   `ts-log/src/codes.ts`; the include_str! roster test now ALSO pins count
   33 and the adjacency). `fail_of_log` is exhaustive over P04R's landed
   `LogError`: `MaintenanceRequired { count, bytes }` maps to a STRUCTURED
   frame `{_tag, count: BigInt, bytes: BigInt, detail}` and
   `MaterializationStale` to `{_tag: "MaterializationStale", detail}` whose
   detail IS the hydration routing (close and reopen the history /
   re-acquire the tenant; recovery hydration is native — the TS layer never
   repairs the cache).
3. **Structured frame renderer for every declared reason.** The frame
   renderer previously spelled EVERY protocol reason `{_tag, detail}`,
   which the TS `ProtocolReason` union would refuse into a core `Internal`
   for the field-structured reasons ("a malformed reason never forges a
   code"). `LogFail` gained a `Structured(StructuredReason)` arm rendering
   exactly the fields `ts-log/src/errors.ts` declares —
   `Contention{attempts: number}`, `NotYetAvailable{requestedSeq: BigInt,
   capturedSeq: BigInt}`, `InsufficientLocalDisk{requiredBytes: BigInt,
   availableBytes: BigInt}`, `MaintenanceRequired{count: BigInt, bytes:
   BigInt}`, `MigrationRepository{path, detail}`,
   `MigrationIntentRequired{requirements: [{code, relation, field:
   string|null, detail}], truncated}` — through ONE shared
   `set_frame_fields` used by both the thrown frame and the embedded DATA
   frame (submit/admin arms), so both spellings are identical. `detail`
   rides along as excess diagnostic data (Effect struct decode ignores
   excess properties). The plain `protocol()` constructor now
   debug-refuses the six field-structured codes, so an undecodable frame
   cannot be emitted by construction. Emission sites converted: the two
   published-snapshot `NotYetAvailable` refusals now carry the REAL
   `{requestedSeq, capturedSeq}` (verified-head tip vs captured local
   stamp; requested at-least stamp vs captured local stamp), and the two
   cache-acquire `Contention` refusals carry `attempts: 1` (one bounded
   acquisition attempt was consumed — recorded decision).
   `MaterializationStale`/`MigrationDrift`/`MigrationUnsupported` need only
   `{detail}`, which the plain frame already carries.
   `InsufficientLocalDisk`/`MigrationRepository`/`MigrationIntentRequired`
   have NO native producer yet (the latter two are the TS generator lane's;
   no bridge lane measures local disk) — their renderer arms exist so any
   future native emission is well-formed, and the variants are marked
   `#[allow(dead_code)]` with that reason.
4. **runtimeTake double-take behavior PINNED (P12's F3 note, null vs typed
   throw): the typed refusal.** The first take spends the operation; every
   later take of the same handle THROWS the typed core `SpentHandle`
   refusal and never returns `null` — `null` is reserved for a payload-less
   completion (`Output::Ready`), so silence can never be mistaken for a
   spent handle. This is `Runtime::take`'s one path, so the contract holds
   for every `*Take` verb (db and log bridges included). Pinned by the doc
   contract on `runtime_take` (runtime_wire.rs), the pin comment on the
   existing `retained_completion_keeps_reservations_until_taken` assertion
   (runtime/tests.rs), and recorded here; the TS declaration
   `runtimeTake(operation): Uint8Array | null` is already consistent (the
   union's `null` is the Ready payload, and errors throw).

Cross-lane notes from this pass: P08's TS `errors.ts`/`codes.ts` needed NO
change (their wave-D pass already landed the 33-row roster and the
structured schemas this pass now satisfies). The wave-D P08 hub requests 7
and 8 are discharged; P04.md hub request 5 (submit_with per-call options +
read-side catch_up wiring into the snapshot `latest` lane) was discharged by
the wave-E F2 closure below (defects 4/8 RESOLVED).

## Wave-E F2 closure (2026-09-05): P04.md hub request 5 — per-call submit
## options + read-side catch-up wiring

Files changed: `ts/crate/src/log_wire.rs`, `ts/crate/src/log_wire/tests.rs`.
Verification: NotRun.

1. **Defect 8 DISCHARGED — the submit arm's wire options reach the machine.**
   `history_verb_in`'s submit arm now reads the C09 `SubmitOptions` wire
   fields exactly as `ts-log/src/native.ts` spells them on
   `HistoryRequestWire` — `attempts` / `backoffBaseMillis` /
   `backoffCapMillis`, JS numbers (the TS machine bounds them pre-dispatch:
   `checkedCount(attempts, 0xffff)`, `checkedNonNegative(millis,
   0xffffffff)`) — through a new `optional_number` reader (absent/null ⇒
   `None`, per the file's existing optional-field discipline; type bounds
   only: finite non-negative integral u32 via `marshal::ordinal`). The pure
   mapping `submit_options_of` builds `writer::SubmitOptions { attempts,
   backoff_base, backoff_cap }` — absent fields fall back to the machine
   defaults (`SubmitOptions::DEFAULT` fields), present values cross
   VERBATIM: the hosted machine itself clamps attempts to `1..=configured`
   and every backoff delay to `MAX_BACKOFF` (P04R2's `effective_attempts`/
   `backoff_delay`), and the bridge never re-clamps beyond the wire's u32
   type bounds. `HistoryVerb::Submit` carries the options and
   `HistoryKind::submit_with` dispatches: Hosted →
   `HostedHistory::submit_with(command, options, work)`; Local →
   `LocalHistory::submit(command, work)` with the options ACCEPTED AND
   IGNORED — the recorded P04R2 contract (P04.md hub request 5:
   "LocalHistory ignores them: one LMDB transaction, no CAS loop"), so no
   unknown-field refusal applies (the fields are known wire data on both
   backends; the arm's convention of reading declared fields is unchanged).
   The old option-less `HistoryKind::submit` delegate is deleted (the one
   caller passes options; tests spell `SubmitOptions::DEFAULT` explicitly).
2. **Defect 4 DISCHARGED — the hosted `latest` lane catches up once before
   refusing.** `open_published_snapshot` is restructured over a new
   `pin_authority_frame` helper (pin the coherent generation + decode the
   attachment captured INSIDE the pinned frame — the same body as before,
   now reusable). On a hosted `consistency: latest` read whose pinned local
   decision differs from the verified head's tip, the lane now: tears down
   the pinned session, invokes P04R2's public `HostedHistory::catch_up`
   under the operation's OWN WorkContext (charged, cancellable, deadline
   shared with the read), re-acquires a lease and RETAKES the pinned frame
   once, then judges freshness with the pure `latest_reached(reached,
   retaken)`: a retaken stamp at (or past — a racing local commit that
   itself followed a head advance) the reached tip is `latest`; still
   behind refuses the STRUCTURED `NotYetAvailable {requestedSeq:
   reached.seq, capturedSeq: retaken.seq}` with the fresh numbers; a
   diverging hash at the caught-up height refuses `Corruption` (the
   materialization disagreeing with the chain it just applied). Exactly ONE
   catch-up attempt per read operation — no hidden repair loop.
   `MaterializationStale` from catch_up (warm cache older than the durable
   tail's checkpoint base) surfaces through `fail_of_log` as its typed
   rostered reason whose detail IS the hydration routing — recovery
   hydration stays native-owned on the next history open / tenant acquire.
   The `Cached`/`AtLeast` lanes and the local `latest` lane are
   byte-for-byte the previous behavior over the shared helper.

Recorded decisions (this closure):
- **Local accepts-and-ignores per-call submit options** — the recorded
  P04R2 contract, not a wire refusal; both backends accept the same submit
  arm.
- **Bridge maps wire options verbatim** — absent ⇒ machine defaults;
  clamping is the machine's judgment alone (no re-clamp in the bridge
  beyond the u32 type bounds the wire already enforces).
- **Post-catch-up judgment accepts reached-or-fresher** — the freshness
  claim is verified against the tip the one catch-up attempt reported; a
  head that advanced again after that attempt is the next read's business
  (one attempt per operation, structurally — no loop exists).

## Authored tests (paths/names → audit/gate IDs) — Verification: NotRun

NEW this pass (all engine-backed, below the N-API layer; no doubles):

`ts/crate/src/db_wire/tests.rs`:
- `sealed_results_outlive_their_session_and_collect_leaves_the_backing` —
  execute-to-sealed inside the pinned session; the result is owned and
  independent (source session drains Closed first); zero-cap collect
  refuses `ResourceLimit{resultBytes}` BEFORE materializing; repeated
  collect answers (backing retained). → API-07, Q-ATOMIC/Q-LIFETIME
  bridge halves, C05.
- `one_shot_transfer_spends_the_result_and_pages_are_byte_bounded` —
  atomic spend; second transfer AND collect-after-transfer refuse
  `SpentHandle`; 1-byte page cap still delivers ≥1 row/page with the
  overflow row buffered (never dropped/doubled); terminal EOF is sticky;
  closed cursor refuses `ClosedHandle`. → API-07/API-10, C05 transfer
  states, FFI double-release shape.
- `drafts_charge_a_cumulative_budget_and_finish_normalizes_add_wins` —
  the SECOND chunk sees the first chunk's spend (cumulative, never reset);
  exhaustion SPENDS the draft (later ingest/finish refuse `SpentHandle`);
  add-wins over delete of the identical fact in one command; finish
  consumes. → API-01/API-10, E-DELTA native half.
- `apply_is_witnessed_judged_and_refuses_a_second_writer` — stale witness
  is the `moved` DOMAIN outcome; foreign-store witness refuses typed;
  accepted-then-no-change (set semantics); the write-session admission
  flag fences apply with `WriterBusy` (refusal, never a parked worker). →
  API-02/03, RUN-05, E-ADMIT bridge half.
- `the_row_codec_is_the_change_set_grammar_and_refuses_foreign_records` —
  encodeRows/decodeRows are the ChangeSet grammar (dedupe + canonical
  order); foreign-relation payloads and tampered bytes refuse typed. →
  API-09/F-WIRE native half.

`ts/crate/src/log_wire/tests.rs`:
- `the_protocol_roster_pins_ts_log_codes_exactly` — the Rust speller and
  ts-log/src/codes.ts compared literal-for-literal in order (the roster
  test's native twin); wave-E: also pins count 33 and the
  MaintenanceRequired→MaterializationStale adjacency. → OPS-006, C10
  pinning.
- `the_result_codec_is_the_core_authority_and_id128_crosses_as_hex`
  (wave-E; replaces `result_records_are_canonical_sorted_and_strict`) —
  the bridge bytes equal `bumbledb::canonical::result::encode_result`
  byte-for-byte (one codec under the digest); JS key order never changes
  the record; a tag-8 Id128 cell decodes (never Corruption) and its JS
  spelling is canonical 32-lowercase-hex; duplicates/truncation refuse;
  empty is empty. → API-06, C12 one-codec discipline.
- `structured_protocol_reasons_carry_their_declared_fields` (wave-E) —
  `fail_of_log` maps `MaintenanceRequired{count,bytes}` to the structured
  payload intact and `MaterializationStale` to the reopen/recovery detail
  (exhaustiveness over LogError is compile-checked). → OPS-006, C10
  structured-frame pinning.
- `local_create_open_and_identity_refusals` — open-never-creates
  (`DatabaseMissing`); checked-artifact create; reopen identity;
  `ForeignIdentity`/`WrongLineage`; wrong-schema open refuses through the
  engine's own fingerprint check. → chapter 35 open/create rows, PROTO-19
  native half, REC/SDK identity rows.
- `create_retry_completes_and_strangers_refuse` — a matching creation
  retry validates the stable identity and completes; strangers refuse;
  an empty artifact refuses `UnsupportedArtifact` before any write. →
  OPS-006 certainty, chapter 30 creation row.
- `submit_decides_and_identity_mismatch_is_not_submitted` — a real sealed
  command decides through LocalHistory; a foreign-scope command is
  NotSubmitted with the typed `ForeignIdentity` frame (never a forged
  receipt). → PROTO-02/05 native halves, API-04.
- `published_snapshots_pin_provenance_and_consistency_refusals_are_typed`
  — the snapshot's provenance comes from the attachment captured INSIDE
  the pinned frame; local latest by construction; unreachable at-least
  refuses the STRUCTURED `NotYetAvailable{requestedSeq, capturedSeq}`
  (wave-E: the test asserts both fields) and the pinned session tears
  down. → chapter 30 published reads, RUN-01.
- `per_call_submit_options_cross_verbatim_and_local_accepts_and_ignores_them`
  (wave-E F2 closure) — the pure wire→machine mapping: absent fields are
  the machine defaults, present values (a hostile-wide request included)
  cross verbatim with NO bridge re-clamp; engine half: a LocalHistory
  submit through `submit_with` under a narrowed per-call budget still
  decides (the recorded accepted-and-ignored contract). → PROTO-04 bridge
  seam, C09 SubmitOptions, P04.md hub request 5.
- `latest_reads_judge_one_catch_up_then_refuse_typed` (wave-E F2 closure)
  — the pure post-catch-up judgment: reached tip accepts, a fresher
  retaken stamp accepts, still-behind refuses the STRUCTURED
  `NotYetAvailable` carrying the reached/retaken seqs, a diverging hash at
  the caught-up height refuses `Corruption`; `MaterializationStale` keeps
  its typed rostered reason through the same lane (recovery hydration
  native-owned). The hosted lane itself is S3-typed
  (`HistoryKind::Hosted` binds `Arc<S3Store>`), so its process-level
  exercise is the P05/P12 real-backend lane over this same seam. →
  chapter 30 published reads, PROTO-06/12 read half, RUN-01.
- `closed_history_refuses_and_close_joins_idempotently` — the spent
  capability refuses `ClosedHandle`; double close joins; the directory is
  reusable by a successor after teardown. → RUN close family, SDK-004/007.
- `planned_target_incarnations_are_deterministic_and_operation_scoped` —
  stable pre-dispatch refs; retries resume the same target. → MIG-08/10
  bridge half.

`ts/crate/src/migration_wire.rs::tests` — strict envelope grammar (floats/
negatives/leading zeros/dup keys/trailing refuse), escape round-trips,
fresh-chain base-prefix agreement with the native digest fns. → TS-MIG-01
native envelope half, C11.

Preserved from earlier passes: the engine-backed reactor tests
(`runtime/session.rs::tests`), pool/directory tests (`runtime/tests.rs`),
tenants.rs registry tests, fingerprint_lock, tags golden, log-identities
golden. Coverage ownership unchanged: REP-009; SDK-002/004..007/010..012;
OPS-004; PERF-005; RUN-01..15 + FFI-01..08 primary; now also the native
halves of OPS-006, PROTO client-side, TS-MIG native-envelope rows.

## Unresolved source dependencies and known defects

1. **P03R (api/prepared — exact requested patches; coded against):**
   - `result.rs`: `pub(crate) fn execute_complete` → `pub fn
     execute_complete` (the bridge's one sealed-execution entry).
   - `CompleteResult`: add `#[must_use] pub fn byte_len(&self) -> u64 {
     self._charge.as_ref().map_or(0, ByteReservation::bytes) }` and
     `pub fn rebind_work(&mut self, work: &WorkContext)` (swap the scratch
     tier's ledger for subsequent reads); same two on `ResultCursor`.
     WITHOUT rebinding, retained results' scratch reads charge the
     original execute operation's ledger, whose DEADLINE eventually
     expires — later collects/pages would refuse `DeadlineExceeded`
     spuriously (a real defect for chapter 35's retained results, not
     just a convenience). `ScratchRelation` needs the matching
     `pub(crate) fn rebind_work(&mut self, work: &WorkContext) {
     self.work = work.clone(); }`.
2. **P01R (changes.rs/canonical.rs — exact requested patches; coded
   against):** make `ChangeSet::records()` and `ChangeRef` (with pub
   fields) `#[doc(hidden)] pub`, and `canonical::{decode, DecodedRow}`
   `#[doc(hidden)] pub` — decodeRows iterates the sealed records through
   the ONE canonical row decoder (duplicating either was rejected as a
   second-codec brand).
3. **Receipt rejection evidence → typed Violation rows.** `ReceiptWire`'s
   `invariant-rejected` arm currently renders `violations: []`: the stored
   `RejectionEvidence` is P04's provisional framed bytes and no canonical
   Violations codec exists to decode them (P01R's wave-C task). When it
   lands and P04 adopts it, `log_wire::receipt_wire` decodes into the same
   `ViolationWire` rows the core uses. Until then a decided rejection's
   receipt is intact evidence with an EMPTY diagnostic list — recorded,
   not silent (this note is the record).
4. **RESOLVED (wave-E F2 closure) — hosted read-side catch-up.** P04R2
   landed the public `HostedHistory::catch_up` and the bridge's hosted
   `consistency: latest` lane now invokes it ONCE (under the operation's
   WorkContext) before refusing, then retakes the pinned frame and judges
   with `latest_reached`; `MaterializationStale` still surfaces typed
   (recovery hydration native-owned on the next open). Details in the
   wave-E F2 closure section above.
5. **Hosted migration execution + hosted restore re-publication** refuse
   typed `MigrationUnsupported` (the staged hosted data plane is P05's
   recorded C08 boundary; P09 recorded the same). Local lanes are
   complete.
6. **Genesis-root backups** (checkpoint: None — whole-chain tails) refuse
   restore with typed `UnsupportedArtifact` until the chain-only restore
   composition lands (C08).
7. **`activatedNow` in migration-activate** is a diagnostic that cannot
   distinguish this-call-activated from recorded evidence
   (`ActivateReport` carries no arm for it); reported true when the
   marker is present. Cosmetic; recorded.
8. **RESOLVED (wave-E F2 closure) — SubmitOptions attempts/backoff.**
   P04R2 landed `writer::SubmitOptions` + `HostedHistory::submit_with`
   and the submit arm now reads `attempts`/`backoffBaseMillis`/
   `backoffCapMillis` from the wire and passes them through verbatim
   (absent ⇒ machine defaults; the machine clamps — the bridge never
   re-clamps). LocalHistory accepts and ignores them per the recorded
   P04R2 contract. Details in the wave-E F2 closure section above.
9. **Protocol Limits / migration cap / planned target-incarnation
   derivation are provisional until the F3 format freeze (C12)** —
   `log_wire::LIMITS`, `migration_wire::MIGRATION_CAP`, the
   target-incarnation domain hash. (The `bumbledb.result.v1` bytes are no
   longer this packet's provisional surface: the wave-E close pass deleted
   the local twin and the ONE codec is the core's
   `bumbledb::canonical::result` — its C12 freeze is P01's row.)
10. **`ts/crate/log-identities.json` golden** — unchanged obligation
    (P05/P12 regenerate; P06 does not edit the golden).
11. Mid-cutover red (expected, owed green at F3): everything above plus
    the hub patches below; `napi` surface details (Either returns,
    `External<T>` as an Object property in `logHistoryCall`'s submit arm,
    `Object::keys`) are coded to the napi3 API and get their first
    compile at F3 like the rest of this phase.

## Hub patch requests (P00 applies)

1. **`ts/crate/Cargo.toml`** (P00): the log machine now drives the store
   backends natively — enable bumbledb-log's default features:
   ```diff
   -# The grammar core only: the store feature (object_store + tokio) stays
   -# out of the cdylib; IO lives in the TypeScript driver.
   -bumbledb-log = { path = "../../crates/bumbledb-log", default-features = false }
   +# The whole internal machine, including the store backends (FsStore/S3):
   +# all storage work is native (C07); no JS transport exists.
   +bumbledb-log = { path = "../../crates/bumbledb-log" }
   ```
2. **To P03R** — apply defect 1's three-symbol patch (visibility +
   byte_len/rebind_work).
3. **To P01R** — apply defect 2's two-file visibility patch.
4. **To P07** — regenerate `ts/test/fixtures/tags.json` from the updated
   `wire_tags!` tables (openKind gains `destinationExists`; NEW
   `scalarExpr` + `numericCast` tables; the wire-tags test's relaxed
   openKind pin can retighten to exact equality). `query/lower.ts` may now
   add the computed-find builder over the `compute` FindTermIr arm
   (P07's recorded boundary 2 — the wire exists).
5. **To P08 (ts-log/src/native.ts + machine.ts — wire-gap extensions the
   native side already reads; all OPTIONAL fields, absent ⇒ typed
   `Misuse`/`UnsupportedArtifact` not-started refusals):**
   - `PlansWire` gains `snapshots?: readonly string[]` — the canonical
     `schema_file::render` texts, BASE schema first then each entry's
     target (entries+1 rows). Required by migration-initialize/-migrate
     (`SuffixRequest` needs real descriptors; digests alone cannot
     reconstruct them). P10's generator already writes these snapshots to
     `meta/`; the runner input must carry them.
   - `AdminRequestWire` binding-carrying arms gain `schema?: unknown`
     (the lowered core SchemaSpec — same value history open sends): needed
     whenever the native side must open the local materialization and the
     tenant is not already open in the registry (checkpoint, pin/release
     root, rotate, retire, erase-local, migration verbs).
   - `restore` gains `backup: string` (the BACKUP operation id — manifests
     are operation-scoped at `<dest>/backup/<op>/manifest`) and
     `schema?: unknown`; `verify-backup` gains `backup: string`.
   - `migration-activate`/`migration-abort` gain `binding?: BindingWire`
     (the SOURCE binding — locates the stable targets namespace
     `<dir>/targets`) and `schema?: unknown` (the TARGET descriptor).
6. **DISCHARGED** — the read-side hosted catch-up seam (defect 4): P04R2
   landed `HostedHistory::catch_up` and the wave-E F2 closure wired it into
   the snapshot `latest` lane. No P04R action remains.
7. No `ts/src/index.ts` / `ts/package.json` change is required by P06
   beyond what P07/P08/P10 already requested (their recorded barrel
   patches cover `internalMigrationSchema/Read`, `cellOf`/`cellBytes`,
   etc.).

## Cross-review challenge / resolution

- Challenge: does the log machine create a second directory-ownership
  authority beside the runtime registry? Resolution: no — the recovery
  machine's kernel fence is INSTALLED into the one registry
  (`reserve_owner_slot`/`install_owner_lock`), so exactly one owner entry
  holds each lock and teardown flows through the one cleanup lane; a slot
  that began closing during the fence acquisition refuses and the lock
  releases through cleanup, never a leaked inode.
- Challenge: is returning `violations: []` for a stored rejection receipt
  a silent loss? It is a RECORDED loss (defect 3) with a precise unblock
  (P01R codec + P04 adoption); the alternative — refusing to decode the
  receipt — would convert durable evidence into an error, which chapter 30
  forbids more strongly.
- Challenge: does the exec-session close (`Closed` by construction)
  under-report in-flight execute jobs? The capability itself holds no
  native resource — its jobs are ordinary registered session operations
  owned by the SNAPSHOT, whose close/drain joins them; the spent flag
  stops new admission immediately. Bounded decision recorded.
- Challenge: teardown jobs self-reclaim by cancelling their own context —
  can that rewrite a completed teardown into a failure? No — the report
  is decided by the `done` flag set AFTER the work ran, before the
  cancellation; the post-work rewrite only affects the never-taken output.
- Challenge: is the migration `snapshots` wire extension a second schema
  channel? No — the texts are the SAME canonical `schema_file::render`
  bytes the generator wrote to `meta/`, parsed by the same native
  grammar; nothing is re-derived in TS.

## Verification

**NotRun** (F1; no tests/typechecks/builds executed per the phase rule).

## F3 note (R3a, 2026-09-05) — ts/crate bridge gate

Brief R3a (F3 stage-3, P06 lane) ran the three bridge gates:

- `cargo check --manifest-path ts/crate/Cargo.toml --all-targets` — **green**
  (0 errors, 0 warnings), verified against a warm cache AND on a forced
  recompile (`touch src/lib.rs`).
- `cargo clippy --manifest-path ts/crate/Cargo.toml --all-targets --
  -D warnings` — **green**, also re-verified on a forced recompile.
- `cargo fmt --manifest-path ts/crate/Cargo.toml` — no changes
  (`--check` clean).

The brief's snapshot (66 unique errors at the wave-C/E surfaces' first
compile) was stale by dispatch time: the working tree already carried
uncommitted repairs across all 18 ts/crate files (a prior repair pass in
this shared tree; the campaign runs parallel agents on one tree). R3a's
work was therefore verification, not repair. Checks performed before
declaring green:

- The frozen TS wires (`ts-log/src/native.ts`, `ts/src/db-native.ts`,
  `ts-log/src/codes.ts`) are UNTOUCHED in the working tree — the Rust side
  conformed, not vice versa.
- The authored coverage stands: all 5 db_wire tests and the full log_wire
  roster named above are present by name (13 fns in db_wire/tests.rs, 18 in
  log_wire/tests.rs), including the codes.ts include_str! roster pin with
  the count-33 + MaterializationStale-adjacency assertions.
- No `unsafe impl Send/Sync` anywhere in ts/crate/src (the only match is
  the db_wire.rs comment forbidding one; the compile locks stand).
- `ts/crate/Cargo.toml` carries exactly hub patch request 1 (bumbledb-log
  default features enabled — the native store backends); no other manifest
  change.

No crates/ edit was needed; no core/log symbol was found missing. Tests
were NOT run per the F3 brief (compile gates only). Judgment call recorded:
finding the lane already green, R3a verified rather than re-derived — a
forced recompile plus the frozen-wire/coverage/unsafe spot-checks above
were judged sufficient evidence for the gate claim.
