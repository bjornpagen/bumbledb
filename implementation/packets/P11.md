# P11 — Lean denotation and independent semantic/history models

Packet / owner / phase: P11 / implementation agent (session 2026-09-04,
resumed same day by a second P11 agent) / F1.
Verification: **NotRun** (F1 rule: no lake/lean/cargo/typecheck executed).

RESUME PASS (second agent, 2026-09-04): audited the first agent's file
state against the packet claims (all confirmed present, test counts match,
no sorry/admit/axiom/native_decide in `lean/`, model independence verified
by import inspection), then completed the remaining fresh-premise removal:
P01 has landed `FieldDescriptor { name, value_type }` with NO generation
attribute (the `Generation` enum is gone from `bumbledb-theory` and the
core re-export), so every remaining `Generation`/`generation:` use in
P11-owned bench files was stale broken source. Removed in this pass —
see "Files owned and changed" additions below.

## Files owned and changed

Lean (all of `lean/` is P11's):

- DELETED `lean/Bumbledb/Txn/Fresh.lean` (whole mint/allocation model) and
  `lean/Bumbledb/Txn/Braids.lean` (`ComponentClosed`, L9, L10).
- NEW `lean/Bumbledb/Txn/Support.lean` — mutable consulted-relation support
  (ASS-001 successor), one-command normalization/tie-rule, raw commutation
  at its real strength.
- NEW `lean/Bumbledb/Float64/Sum.lean` — exact sum/mean accumulator: merge
  table assoc/comm/identity, non-idempotence, permutation/partition
  independence, proved 34-limb bound, special-case table, mean exact-rational
  replication law, executable ties-to-even rounding with kernel `#guard`
  goldens ({1e16,1,-1e16}, {MAX,MAX} sum-vs-mean, subnormal pair, both tie
  directions, canonical zero), min/max by total order.
- NEW `lean/Bumbledb/FloatInterval.lean` — dense `Interval<F64>` model:
  checked structure (NaN refusal, strict numeric order, infinity placement
  as theorems, `[-0,+0)` refusal), dense nonemptiness incl. the
  `[-Infinity,-MAX_FINITE)` witness, exact-embedding membership with the
  order-key execution bridge and the nonfinite-probe-guard countermodel,
  adjacency coalescing vs representable-gap theorems, `LinearElem F64`
  instance (extends the existing proved sweep/pack algebra to float
  endpoints), measure with distinct Unbounded/Overflow and `#guard` goldens.
- NEW `lean/Bumbledb/Query/Stages.lean` — staged nonrecursive composition
  model: stages read declared inputs only, producer errors surface through
  consumers, unreferenced stages invisible (naming ≠ materialization),
  inlining preserves values AND errors, frozen-finite-domain recursion
  containment induction with aggregate/computed predecessors admitted, the
  value-creation-escapes countermodel, kernel-checked grain fixtures
  (students-vs-attempts, equal amounts on distinct bindings, empty input no
  group, error-through-filter graph).
- `lean/Bumbledb/Capacity.lean` — appended successor obligations:
  `zero_weight_membership_distinct` (weighted zero is not absence) and
  `natSum_widened_bound` (u128 widened fold never wraps under the u64
  count/weight domain), plus `natSum_map_zero`/`natSum_le_length_mul`.
- `lean/Bumbledb/Bridge.lean` — removed the 5 fresh/braid rows; added 17
  successor rows with term-level references (Support ×4, F64.Agg ×4,
  FloatInterval ×4, Stages ×5); `ledger_count` 104 → 116; imports updated.
- `lean/Bumbledb.lean` — imports: − Fresh/Braids, + Support, Float64.Sum,
  FloatInterval, Query.Stages.
- `lean/Bumbledb/Query/Syntax.lean` — creation-quarantine doc rewritten
  without the deleted mint model.
- `lean/README.md` — refinement-chain text: successor modules added,
  fresh/braid retirement recorded.
- NEW `lean/proof-bridge-ledger.md` — the required bridge ledger:
  guarantee → model statement → premises → construction site → independent
  fixture/gate → named unsupported conditions, for every P11 surface, with
  every empirical gap named explicitly (FPU environment, IEEE hardware
  correspondence, rounding nearest-ness beyond goldens, planner/support
  correspondence, engine limb representation, alias-normalization table).

Bench (P11-owned dirs; module-root files of those dirs edited as the
directories' mod anchors):

- NEW `crates/bumbledb-bench/src/verify/f64_oracle.rs` (+ `f64_oracle/tests.rs`)
  — independent bit/bignum/rational binary64 oracle: canonicalization, total
  order key, scaled decomposition, `Wide` bignum, exact accumulator with the
  canonical merge table, dyadic ties-to-even rounding, sum/mean, and
  correctly rounded reference add/sub/mul/div. Tests:
  `golden_bits_match_host_literals`, `canonicalization_collapses_zeros_and_nans`,
  `order_key_total_order_matches_numeric_classes`,
  `scaled_magnitude_decomposition_is_exact`,
  `exact_sum_matches_rational_oracle`,
  `sum_is_permutation_and_partition_independent`,
  `partial_state_replay_is_not_idempotent`,
  `mean_divides_exact_rational_not_rounded_sum`,
  `ties_round_to_even_at_binade_boundaries`,
  `reference_arithmetic_matches_host_on_goldens`,
  `reference_arithmetic_ieee_special_table`,
  `accumulator_count_tracks_cardinality_exactly`.
- NEW `crates/bumbledb-bench/src/verify/finterval_oracle.rs` — dense float
  interval oracle (representable + just-above dense fragment): constructor
  refusals, membership with the finite-probe guard, raw-key-comparison
  divergence, coalescing/gap, measure roster. Tests:
  `constructors_parse_and_refuse`, `neg_inf_to_neg_max_ray_is_nonempty`,
  `order_key_execution_matches_dense_membership`,
  `nonfinite_probes_return_false`,
  `adjacent_coalesces_and_representable_gap_does_not`,
  `measure_distinguishes_unbounded_overflow_and_length`.
- NEW `crates/bumbledb-bench/src/naive/successor.rs`,
  `naive/successor/admission.rs` (independent `mutable_support` derivation +
  tests `judgment_stable_under_untouched_relations`,
  `shared_closed_vocabulary_does_not_merge_supports`,
  `same_command_tie_rule_add_wins`,
  `raw_commutation_does_not_commute_admission` — incl. the key/capacity
  union-closure counterexamples of chapter 02),
  `naive/successor/staged.rs` (independent staged evaluator + tests
  `consumer_filter_cannot_hide_producer_error`,
  `naming_does_not_force_materialization`,
  `inline_and_materialized_stages_agree`,
  `aggregate_grain_is_the_distinct_input_row_set`,
  `mean_of_means_is_not_the_global_mean`,
  `frozen_computed_predecessors_stay_in_domain`,
  `value_creation_feedback_is_refused`,
  `later_stages_cannot_rewrite_earlier_outcomes`).
- NEW `crates/bumbledb-bench/src/closure/history_model.rs`
  (+ `history_model/tests.rs`) — the independent tiny history model
  (HEAD CAS over immutable decisions, receipts/epochs/retirement,
  ExactState/ABA, Frozen/Deleted with the exact guard ordering, structural
  command-identity binding — no digest and no bumbledb-log import), the
  `verify_trace` observation checker for P12, and the adversarial schedule
  corpus: `proto_01_one_successor_per_revision_under_every_interleaving`
  (all 6 two-writer interleavings + linearization check),
  `proto_02_same_id_same_body_is_one_stable_outcome`,
  `proto_03_same_id_different_body_conflicts_without_execution`,
  `proto_08_two_witnessed_decrements_one_precondition_failure` (+ blind
  variant), `proto_09_state_stamp_moves_exactly_on_net_change` (ABA),
  `proto_16_rotation_freeze_and_retirement`,
  `retirement_cannot_touch_the_open_epoch`,
  `deleted_refuses_ordinary_access`,
  `bounded_attempts_return_outcome_unknown_not_a_lie`,
  `trace_checker_rejects_double_winners_and_forked_outcomes`.
- NEW `crates/bumbledb-bench/src/corpus_gen/float_corpus.rs` — deterministic
  float fixture corpus generators (canon/order/arith/agg) with
  oracle-computed expectations and a fixed line-hex rendering; two authored
  tests. **Regeneration deferred to F3** (see below).
- Fresh-premise removal in owned files: `lawful.rs` + `lawful/lanes.rs`
  (application-owned cursor ids replace `tx.reserve`; three declared id
  keys replace fresh auto-keys), `lawful/enforcement.rs` (map reordered:
  closed autos first; `declared id key` rows), `lawful/tests.rs` (counts:
  7 declared keys, 0 generated fields), `closure.rs` (`Node(id) -> Node`
  declared key), `differential/tests/ground.rs` and
  `differential/tests/fold.rs` (explicit id fields + declared Functionality
  statements at the historical statement positions),
  `conformance/judgment.rs` (retired R16 fresh-interplay note).
- Module-root mod additions: `verify.rs` (+f64_oracle, +finterval_oracle),
  `naive.rs` (+successor), `closure.rs` (+history_model),
  `corpus_gen.rs` (+float_corpus).

Resume-pass changes (Generation purge — the enum/attribute no longer
exists in production; these were compile-broken stale premises):

- `naive/tests/judgment.rs` — dropped the `Generation`/`FieldDescriptor`
  imports; the "Target" relation's `id` field was the last
  `Generation::Fresh` premise in P11 dirs — now an ordinary declared field
  via `fixture::field` (E-NO-RESERVE comment records the change).
- `conformance/judgment.rs`, `conformance/complete.rs` — local `field()`
  helpers and imports lose the generation member.
- `corpus_gen/theorygen.rs` — `random_field` no longer draws a 1-in-5
  fresh generation (the draw is deleted WITH the attribute, not preserved
  as dead entropy); NOTE this shifts the RNG stream, so checked-in corpora
  are stale until the deferred F3 regeneration (already recorded).
- `corpus_gen/theorygen/arity.rs` — three `generation: Generation::None`
  struct fields and the import removed.
- `lawful/tests.rs` — the `fresh_fields == 0` runtime assertion replaced
  by a comment: the property is now compile-time truth (the field does not
  exist on the type), exactly the one-line removal the first pass
  predicted.
- `lean/Bumbledb/Txn.lean` (module doc) — the Recipe-28 narrowing note no
  longer describes the mint as live "id-allocation mechanism"; it now
  records the mint as retired with entity identity application-owned.
- `lean/Bumbledb/Exec/Rewrites.lean` (doc on `KeyProbeShape.declared`) —
  "fresh auto-keys included" corrected to "only declared keys; the retired
  fresh auto-keys are gone with their mint".

## Input contracts received (IDs + actual symbols)

- C01 (P01, concurrent): consumed as chapters 10/11 plus the actual landed
  `crates/bumbledb-theory/src/{float.rs,float/,interval.rs,id128.rs}`
  (canonical F64 quotient/order-key, casts, `Interval<F64>` with
  Unbounded/Overflow measure split, exact `sub_rounded`) and the preserved
  descriptor vocabulary (`SchemaDescriptor`, `StatementDescriptor`,
  `Weight`, `Bound`, `Side`, `Row`). FieldDescriptor in bumbledb-theory has
  no generation attribute (fresh already gone there); the core crate's
  `Generation` remains transitional.
- C03 (P01): chapter 10 §2/§4 + existing `lean/Bumbledb/Capacity.lean`.
- C05 (P03, wave B, not yet produced): staged model coded against chapter 12
  text only; correspondence tests are declared by name and wired in F2/F3.
- C06 (P04, concurrent): consumed as chapter 20 plus the actual landed
  `crates/bumbledb-log/src/history/admission.rs` guard ordering (reviewed
  below); the bench model shares NO symbols with it (deliberate).
- C11 (P09/P10, wave B): chapter 22 text only (Frozen/Deleted/activation
  states in the history model).

## Output contracts supplied (IDs + actual symbols)

- ASS-001 model: `Bumbledb.Txn.Support.{mutableRels, AvoidsMutable,
  judgment_stable_outside_mutable_support,
  holds_stable_outside_mutable_support, disjoint_mutable_locality,
  closed_not_mutable, Delta.normalize, normalize_applyTo,
  normalize_idempotent, add_wins, applyTo_comm_of_disjoint}` and bench
  `naive::successor::admission::{consulted, mutable_support,
  untouched_statements, citations_for}` (the derivation P01/P03's planner
  is compared against in F3).
- Float model: `Bumbledb.F64.Agg.{NumTotal, Acc, fold, fold_perm,
  fold_append, fold_total_bound, accumulator_within_34_limbs,
  merge_not_idempotent, fold_nan_of_mem, fold_mixed_inf_nan,
  fold_posInf_only, fold_all_finite, roundPosRat, roundRatBits, sumF64,
  meanF64, sum_max_max_overflows, mean_replicated_equiv}` and bench
  `verify::f64_oracle::{canonical, order_key, Wide, Acc, fold, round_dyadic,
  sum_bits, mean_bits, ref_add/ref_sub/ref_mul/ref_div/ref_neg}`.
- Interval model: `Bumbledb.{FInterval, FInterval.points,
  FInterval.nonempty, containsF64_iff_orderKey, nonfinite_probe_false,
  neg_inf_probe_needs_guard, FInterval.join_points, gap_uncovered,
  adjacent_endpoint_gap, FInterval.measure, FMeasure}`, `LinearElem F64`
  instance, and bench `verify::finterval_oracle::{FInterval, Dense,
  Measure, measure, coalesce, next_up}`.
- Stage model (ASS-002 query side): `Bumbledb.Query.Stages.{Stage,
  evalGraph, runStage_congr, consumer_of_error, unread_stage_invisible,
  inline_runStage, iterate_subset_dom, value_creation_escapes}` and bench
  `naive::successor::staged::{Stage, eval_graph, recurse_within, …}`.
- History model (ASS-002): bench `closure::history_model::{Model, Command,
  CommandId, StateStamp, Condition, Terminal, Refusal, Lifecycle, Event,
  verify_trace}` — P12's schedule/trace checker; P04 differential target.
- Finite counterexamples handed to P12: `merge_not_idempotent`,
  `neg_inf_probe_needs_guard`, `value_creation_escapes`, the capacity/key
  union counterexamples in `admission.rs`, the blind-vs-witnessed decrement
  pair, and the double-winner/forked-outcome traces `verify_trace` rejects.
- Bridge obligations handed to P00: `lean/proof-bridge-ledger.md` (complete
  guarantee→gap table); `Bumbledb.Bridge.ledger` rows 100–116.

## Implemented behavior

Per the packet: mutable-support theorem premises replaced (ComponentClosed
retired with the braid module); canonical F64 quotient/order already present
was extended with the exact accumulator/rounding proofs; integer/float
interval semantics (dense line, exact endpoints, distinct measure failures,
LinearElem reuse of the proved sweep algebra); grouped exact measures
(zero-weight distinction + widened fold bound added to the existing
empty-parent/zero/window laws); complete query-stage denotation at model
level (aggregate/computed outputs downstream, error/grain/inline laws);
restricted recursive finite domain (containment induction with frozen
computed predecessors + value-creation countermodel). Independent bench
models for admission/query/history with adversarial trace corpus; exact
reductions have independent bit/rational checks in both Lean-kernel and
bench-oracle form. NO sorry/admit/axiom/native_decide anywhere in `lean/`.

## Deleted mechanisms

- `lean/Bumbledb/Txn/Fresh.lean` — the whole mint model (never-reissue,
  resupply, materialized-key riders). No successor allocation theorem
  replaces it, by design: entity identity is application-owned data.
- `lean/Bumbledb/Txn/Braids.lean` — `ComponentClosed`, `L9`, `L10`; their
  Bridge rows (which cited `crates/bumbledb-log/src/braids.rs`, being
  deleted by P04) removed rather than relabeled.
- Bench fresh premises: `tx.reserve` mints in `lawful/lanes.rs`;
  `fresh` markers in `lawful.rs`/`closure.rs` schema blocks;
  `Generation::Fresh` field in `differential/tests/fold.rs`;
  `fixture::fresh` usage in `differential/tests/ground.rs`; the
  "fresh auto-key" enforcement rows and the escaped-high-water/burn
  narrative in `lawful.rs`.

## Authored tests (paths/names → audit/gate IDs)

- `verify/f64_oracle/tests.rs` (12 tests) → F-CANON, F-GOLDEN, F-ORDER,
  F-ARITH, F-AGG, F-OPT-NEG (native-fold divergence fixture), P-FLOAT;
  ASS-002 (independent expectations).
- `verify/finterval_oracle.rs` tests (6) → F-INTERVAL, Q-TEMPORAL (dense
  endpoint oracle), E-VALUE (constructor refusals), P-FLOAT.
- `naive/successor/admission.rs` tests (4) → ASS-001, CONC-01, CONC-02,
  CONC-05, E-DELTA, E-ADMIT, P-SEMANTIC.
- `naive/successor/staged.rs` tests (8) → Q-IR, Q-GROUP, Q-RECUR, F-SET,
  F-OPT-NEG (mean-of-means), ASS-002, P-SEMANTIC.
- `closure/history_model/tests.rs` (10) → PROTO-01/-02/-03/-08/-09/-16
  (model half), CONC-04, ASS-002, G07, P-SEMANTIC.
- `corpus_gen/float_corpus.rs` tests (2) → F-GOLDEN/F-CROSS corpus
  determinism.
- Lean `#guard`/`example` kernel fixtures in Float64/Sum.lean,
  FloatInterval.lean, Query/Stages.lean → F-PROOF, P-KERNEL.
- Existing-lane updates: `lawful/tests.rs` (no generated field remains) →
  E-NO-RESERVE flavor; `differential/tests/ground.rs`, `fold.rs` keep their
  three-way differential coverage under declared keys.

None executed (F1).

## Unresolved source dependencies and known defects

- F3 must run `lake build` + `scripts/spec-census.sh`: the new Lean proofs
  are hand-checked but unverified; highest-risk spots (recorded so the F3
  repairer starts there): tactic-chaining in
  `Float64/Sum.lean:fold_posInf_of_mem`/`fold_negInf_of_mem`
  (`rcases … <;> (simp only … <;> cases … <;> simp …)`), core `Rat` lemma
  name drift in `FloatInterval.lean` (`Rat.add_lt_add_left`,
  `Rat.sub_lt_iff`, `Rat.intCast_*`), `List.getElem?_*` lemma usage in
  `Query/Stages.lean`, kernel-compute depth of the `#guard` goldens
  (maxRecDepth raised to 8192 where used).
- Census mechanism/instrument strings in the new Bridge rows point at
  CURRENT paths (plan.rs/judgment.rs/delta.rs/finalize.rs/interval.rs/
  ir.rs/sweep.rs/allen.rs/error.rs) and at my new bench test fns; if
  P01/P03 move those files the rows need re-pinning (census will flag).
- The engine-vs-model differentials for the successor planner support
  (mutable_support), staged IR (C05) and history machine (C06) are declared
  by test name but can only be WIRED once those packets' symbols integrate
  (F2); the model sides are complete. No fake adapters were written.
- Float corpus regeneration and any Lean-conformance corpus regeneration
  are DEFERRED (generator execution). Deferred commands recorded:
  `cargo run -p bumbledb-bench -- corpus-float --seed 0xB0B --out fixtures/float`
  (needs the cli arm below), then the standard conformance regeneration for
  successor query forms once C05 lands.
- RESOLVED in the resume pass: `lawful/tests.rs` no longer imports
  `Generation` (P01 deleted the enum; the zero-generated-fields assertion
  is compile-time truth now). All P11-owned files are Generation-free.
- Rounding nearest-ness in Lean is an executable spec + kernel goldens, not
  an all-inputs theorem — named in the bridge ledger; the bench oracle and
  F-ARITH carry it empirically. This is a declared scope decision, not an
  accidental gap.

## Review concerns (P01/P03/P04/P09 pair review — no production files edited)

- P01 (bumbledb-theory float/interval/id128): converges with the models —
  canonical quotient, order key, cast roster, `Interval<F64>` with distinct
  `Unbounded`/`Overflow`, `sub_rounded` one-rounding, nonfinite probes
  false. Concerns: (1) `float/cast.rs`/`float.rs` tests use host checked
  conversions as expectations in places — pair them with oracle-computed
  bits (`verify::f64_oracle`) so a shared host assumption cannot mask a
  bug; (2) `exec/kernel/numeric/accumulator.rs` header says the 34-limb
  bound is "not a completed Lean proof" — it now is
  (`F64.Agg.accumulator_within_34_limbs`); update the comment and keep the
  limb layout bit-compared against the model in F3.
- P03 (preserved exec sink, packet not yet dispatched): `F64Math` enters
  the `NumericalGuard` PER OPERATION; chapter 11 wants one guard around a
  whole engine operation, not per tuple — hoist at integration.
  `push_repeated` (constant-group batch) multiplies a contribution by a
  binding multiplicity: legal only under a checked distinct-binding
  witness, because the merge is not idempotent (`merge_not_idempotent` and
  `partial_state_replay_is_not_idempotent` are the fixtures). Interior
  rules remain projection-only in the preserved IR; `Query/Stages.lean` +
  `naive/successor/staged.rs` define the target composition/error/grain
  laws (Q-IR).
- P04 (log history, concurrent): `history/admission.rs` guard ordering
  matches the independent model exactly (identity/Deleted/retirement
  precede lookup; lookup precedes Frozen/closed-epoch; open absence is a
  frontier observation) — good convergence. Concerns: (1) verify
  HeadRevision-moves-always vs StateStamp-moves-only-on-net-change is
  encoded in decision/frame types (the model's proto_09 is the schedule);
  (2) braids.rs/vector.rs are gone but stale references remain in
  `codec.rs`, `identities.rs`, `checkpointer.rs`, `manifest.rs`,
  `inspect.rs`, `replica.rs`, `gc.rs`, `sidecar.rs`, `lib.rs` (P00 hub) —
  the cutover sweep must finish before F2; (3) the model's
  Deleted-refuses-before-lookup rule (tombstones have no receipt table) is
  a boundary worth an explicit production test.
- P09 (not started): the migration executor must reuse the core staged
  evaluator semantics; MIG-13's independent ordered-step oracle should be
  built on the same shape as `staged::eval_graph` (complete outcomes per
  step, error boundaries preserved under fusion) rather than a new
  evaluator.

## Hub patch requests

1. `crates/bumbledb-bench/src/cli` (P14) / entrypoint scripts (P00): add a
   `corpus-float` subcommand arm dispatching to
   `corpus_gen::float_corpus::{canon_corpus, order_corpus, arith_corpus,
   agg_corpus, render_agg}` (seed/out flags as in the module doc). One match
   arm; no other cli changes requested.
2. `crates/bumbledb-bench/src/fixture.rs` (P14/unclaimed) — NOW URGENT:
   the file is compile-broken because `FieldDescriptor` has no
   `generation` member and `bumbledb::schema::Generation` no longer
   exists. Exact requested replacement for lines 1 and 25–38 (the import,
   `field`, and `fresh`):

   ```rust
   use bumbledb::schema::{FieldDescriptor, ValueType};
   // ...
   pub(crate) fn field(name: &str, value_type: ValueType) -> FieldDescriptor {
       FieldDescriptor {
           name: name.into(),
           value_type,
       }
   }
   // DELETE pub(crate) fn fresh entirely.
   ```

   No P11 file uses `fixture::fresh` any more; the remaining callers are
   P14's (`sqlmap.rs`, `querygen/target.rs` — `fresh("id")` there becomes
   `field("id", ValueType::U64)`, an ordinary declared field). Coordinate
   with P14 so the fresh deletion and its caller migration land together.
3. No Cargo/lib.rs/barrel changes needed: all new files are submodules of
   already-declared modules. NOTE the ownership interpretation used: the
   module-root files of P11's dirs (`naive.rs`, `closure.rs`, `verify.rs`,
   `corpus_gen.rs`, `lawful.rs`, plus their subfiles listed above) were
   edited as part of the directory ownership; if P00 reads chapter 62's
   default more narrowly, the exact diffs are in this working tree and are
   confined to mod declarations, fresh-premise removal, and doc text.
4. `crates/bumbledb/src/exec/kernel/numeric/accumulator.rs` (P03/P01): the
   requested one-line doc patch replacing "not a completed Lean proof" with
   a citation of `lean/Bumbledb/Float64/Sum.lean:accumulator_within_34_limbs`
   — apply when that file's owner is active.

## Cross-review challenge / resolution

- Challenge (self, recorded): is a fixed dense-fragment (`Dense::JustAbove`)
  oracle faithful to the dense contract? Resolution: interval endpoints are
  always representable, so every adjacency/gap/membership fixture is decided
  at half-ulp granularity; the fully dense rational model is the Lean side
  (`FInterval.points : Set Rat`), and the two must agree on shared fixtures
  — recorded in both module docs and the bridge ledger.
- Challenge: should L10 (replay idempotence) get a successor replacement?
  Resolution: no — the successor log replays exact immutable decisions at
  their exact predecessors (chapter 20 historical replay), not batches over
  arbitrary states; a replay-forward-heals theorem would re-import the
  retired recovery design. Recorded in the ledger's retired-premises table.

## Verification

NotRun (F1). First F3 commands for this packet: `lake build` in `lean/`,
`scripts/lean.sh` (census), `cargo nextest run -p bumbledb-bench` filtered
to the new modules, then the deferred corpus generation (NOTE: the
theorygen RNG stream shifted when the fresh-generation draw was deleted,
so ALL theorygen-derived checked-in corpora regenerate, not only the new
float files).

Resume-pass audit trail (second agent): confirmed by grep/read — no
sorry/admit/axiom/native_decide in `lean/`; Bridge `ledger.length = 116`
matches the census's self-derived row count (no external hardcode to
patch); model files import no production equality/arithmetic/hash/
transition helper; every P11-owned file is now free of the deleted
`Generation` vocabulary; deleted `Txn/Fresh.lean`/`Txn/Braids.lean` have
no remaining imports (doc provenance mentions only).

## F3 note (R2c bench-green repair)

Bench-side drift repaired to the current successor contract; judgment calls:

- `FindTerm::Compute(ScalarExpr)` is now modeled in the naive oracle
  (`naive/query.rs`): an INDEPENDENT evaluator — integers widened through
  `i128` and narrowed back or refused, IEEE binary64 ops on host `f64`
  canonicalized per node through `F64::from_bits`, exact-or-refused casts
  re-checked by bit-exact round-trip, no mixed-type promotion. A computed
  head acts as a per-row output (joins group keys beside `Var`). Failures
  are the new `QueryError::Scalar { find }`, mapped to the new
  `differential::Answers::Scalar` in `differential.rs`, `conformance.rs`
  and `differential/tests/fold.rs`. Engine-side parity mapping stays the
  existing panic-on-unknown until querygen actually draws Compute.
- The Lean conformance grammar spells no compute head, `id128` or dense
  interval value: such cases are EXCLUDED and counted (new
  `Exclusion::{ComputedHead, UnrepresentableValue}`, `Report::
  {excluded_compute, excluded_value}`), never serialized into bytes the
  Lean reader cannot parse.
- `corpus_digest` gained value tags 9 (`Id128`) and 10 (`IntervalF64`) —
  appended; existing tags never renumber, so old digests stay stable.
- `theorygen::typed_value`/`interval_value` gained honest `Id128` and
  dense-interval generation arms, but `random_type`'s draw pool is
  deliberately unchanged (deterministic corpora stay stable); widening the
  pool to the new kinds is recorded follow-up work.
- `f64_oracle::decompose`: the `cast_possible_truncation` expectation went
  unfulfilled on current clippy; replaced by `u32::try_from(...).expect`.
- `querygen` coverage: new `membership_f64`/`allen_f64`/`point_in_f64`
  counters; `type_index` refuses `Id128` loudly (the target schema
  declares no id128 column — widen the 7-type matrix when it does).

## F3 note (Lean repair — lake build green)

`lake build` is green (52 jobs) after proof-level repair of the F1
hand-written proofs; `lake exe conformance conformance/cases` passes on
the checked-in corpus (277 cases, 0 disagreements — the successor-form
corpus regeneration stays the deferred L14 item, unblocked separately);
the battery 1/2 escape-token greps of `scripts/lean.sh` are clean
(no proof-escape token, no axiom declaration, no native_decide;
verified with the script's exact patterns — the whole script was NOT
run here because it invokes cargo).

Statement-level changes (recorded per the F3 rule; everything else was
tactic/proof repair only, no theorem weakened or deleted):

- `Query/Stages.lean` `inlineStage`/`inline_runStage`: the F1 spelling
  was ill-typed — the consumer stage was written `⟨[k], consume⟩` with
  `consume : List Row → Except Unit (List Row)` where `Stage.eval`
  takes `List (List Row)`, and `inlineStage` fed `consume [rows]`
  (a singleton list-of-tables where a table was wanted). Repaired to
  the typed form: the consumer stage is
  `⟨[k], fun tables => consume (tables.headD [])⟩` and `inlineStage`
  binds the producer output directly (`(p.eval tables).bind consume`).
  Same contract at full strength: a consumer of exactly the produced
  table equals the inlined composition on BOTH the value and error
  paths.
- `Capacity.lean`: the F1-added helper `natSum_le_length_mul` collided
  with the pre-existing `Bumbledb.natSum_le_length_mul` in
  `Query/Aggregates.lean` (same namespace); renamed to
  `natSum_map_le_length_mul` (the weighted/mapped form), statement
  unchanged; `natSum_widened_bound` and the Aggregates lemma keep their
  names.

Proof-only repairs, for the record of what drifted: Capacity
`zero_weight_membership_distinct` / list base case (`0 ≤ 0` vs
`≤ [].length * c` mismatches); Stages `runStage_error_of_read`
(generalize `s.reads` before induction), `consumer_of_error` (rewrite
through `evalFrom_append` in the `evalFrom []` spelling), `iterate_grows`
(`show` through the `iterate` equation), `#guard` matches (spell
`Except.error`/`Except.ok` — dotted constructors do not resolve under
`#guard match`); Float64/Sum `NumTotal.merge_finite_inv` (explicit case
split — `nomatch` under `try` logs instead of failing),
`contrib_finite_bound` (unfold constants in the GOAL too so omega sees
the `% 2^63` literal), `fold_all_finite` (drop unused simp arg),
`sum_max_max_overflows` (`set_option maxRecDepth 65536 in` at the one
declaration — 8192 was not enough for the two kernel `decide`s; the
file-level 8192 still covers every `#guard`); FloatInterval
(P11-pre-flagged core Rat drift): `Rat.le_refl` is now implicit-argument,
and `1/2 : Rat` literals do not kernel-reduce under `decide` (`Rat.div`
normalization) — the dense-gap witnesses are now spelled `mkRat 1 2`,
which reduces; `Txn/Support.lean` `Delta.normalize` was declared inside
`namespace Support` so dot-notation `d.normalize` on `Txn.Delta` could
not resolve — call sites use explicit `Delta.normalize d` (full name
stays `Txn.Support.Delta.normalize`, as the ledger records). Prose-only:
two doc uses of the word "admit" in `FloatInterval.lean` and the
sorry/admit mention in `proof-bridge-ledger.md` reworded — battery 1
greps comments and docs too, and the gate must be clean, not
almost-clean.
