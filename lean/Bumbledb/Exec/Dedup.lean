import Bumbledb.Query.Aggregates

/-!
# Exec/Dedup — seen-set union (Level 1, PRD 07)

Deduplication as set union; the elision licences — the
`DistinctWitness` and `DisjointWitness` theorems; the union regime.
Algorithmic essence only, per the mechanism fence.

This file is a scaffold stub (PRD 01): the theorems land in PRD 07.
-/
