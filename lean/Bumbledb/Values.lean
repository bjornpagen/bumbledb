/-!
# Values — the value universe (Level 0, PRD 02)

The seven-type value universe: the six pure value types plus the
nonempty half-open interval, with order-embedding encodings, rays,
and measure. Types are encodings — hard structural typing; nominal
safety lives in host Rust newtypes, never here.

This file is a scaffold stub (PRD 01): the definitions land in PRD 02.
-/
