import Bumbledb.Dependencies

/-!
# Query syntax — the pure-data IR (Level 0, PRD 04)

The query IR as data: atoms, negation, membership, param sets,
conditions. Syntax only — meaning lives in `Bumbledb.Query.Denotation`.

This file is a scaffold stub (PRD 01): the definitions land in PRD 04.
-/
