import Bumbledb.Query.Syntax

/-!
# Query denotation — the matching equation (Level 0, PRD 04)

What a query means: the matching equation, unification, range
restriction, anti-join, condition lowering (DNF preservation), rule
union, answer identity.

This file is a scaffold stub (PRD 01): the definitions and theorems
land in PRD 04.
-/
