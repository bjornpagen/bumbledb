# Reflection-using module sets (bumbledb.meta.*) land here as they are
# implemented (TODO_CPP §4). This directory is added only in the GCC
# production graph — never in the Clang lint graph (see the top-level
# CMakeLists.txt). No targets yet.
