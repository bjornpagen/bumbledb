# plan-008: Program vocabulary in plan tests and ground comments

- **Severity:** low
- **Tree:** plan
- **Status:** DUPLICATE(engine-011)
- **Source:** audit/plan-exec.md F20 (plan half)

engine-011 is the load-bearing vocabulary sweep. Its `rg -inw 'program'` covers `plan/ground/evaluate/tests.rs:809` (`multi_rule_programs_fold_per_rule_independently`) and `plan/ground.rs:402` prose. Identifier rename `grounded_program` is engine-034 (plan-007). No separate fix lands under this id.
